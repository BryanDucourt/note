总体评价
1 姜金晶 12年还是调库，对业务和技术没有自己的思考 倾向于pass
2 夏汉斌
# 1 姜金晶

### 1. 享坐车中台管理-客户端 (React + TS + AntD)
**考察点：** React性能优化、权限设计、复杂组件封装

*   **问题一：** 简历中提到使用了 `HOC` (高阶组件) 实现了按钮级别的权限控制。请具体讲讲你的实现思路？是如何配合后端接口数据来控制组件渲染的？
    *   **追问：** 如果权限数据量很大，或者需要动态变更权限，这套方案如何保证页面渲染性能？
*   **问题二：** 关于“独立封装Excel导出插件”，是在纯前端完成的吗？如果是，使用了什么库（如 SheetJS）？在处理大数据量（例如几万条数据）导出时，如何避免浏览器卡顿或崩溃？
    *   **追问：** 有没有考虑过使用 Web Worker 来处理耗时的导出计算？
*   **问题三：** 你提到了使用 `useMemo` 和 `React.memo` 优化性能。在实际开发中，你是如何判断一个组件或计算逻辑需要被缓存的？有没有遇到过过度优化导致的问题？

### 2. 天猫校园三亚学院摆渡车 (Uniapp + Vue3 + Websocket)
**考察点：** 实时通信、地图交互、移动端性能

*   **问题一：** 项目中使用了 WebSocket 实现车辆呼叫和位置上传。请问你是如何处理 WebSocket 的断线重连（Heartbeat/Reconnect）机制的？在弱网环境下（如校园死角）表现如何？
    *   **追问：** 当消息并发量较大时，前端如何处理消息堆积或乱序问题？
*   **问题二：** 在高德地图上实现“车辆运行线路绘制及动态描点”，由于车辆位置是离散的点，前端是如何实现车辆平滑移动效果的？
    *   **追问：** 如果司机端上传的 GPS 坐标有漂移，前端做了哪些纠偏处理？

### 3. PC端基础组件库 (Vue3 + Monorepo + Verdaccio)
**考察点：** 工程化、架构设计、组件库建设

*   **问题一：** 你使用了 `pnpm` + `Monorepo` 进行管理。相比于 npm 或 yarn，pnpm 在这个场景下解决了什么核心问题？
    *   **追问：** 在 Monorepo 架构下，如何处理不同子包（package）之间的版本依赖和循环引用问题？
*   **问题二：** 对 ElementPlus 进行二次封装时，主要封装了哪些类型的业务逻辑？如何保持封装后的组件依然具有较高的灵活性（比如透传 Props 和 Slots）？
    *   **追问：** 使用 Verdaccio 搭建私有库时，有没有配置权限管理？CI/CD 流程是如何实现自动发包的？

### 4. 享坐车-车机屏司机端 (Flutter + Websocket)
**考察点：** 跨平台开发、原生交互、性能调优

*   **问题一：** 简历提到“优化启动白屏”。请问你是通过哪些具体手段解决 Flutter 启动白屏问题的？（例如：原生启动图、引擎预热等）
*   **问题二：** 这里提到“使用定时器对 Websocket 请求进行定时轮询”。这看起来有点矛盾，通常 Websocket 是全双工通信，为什么要结合定时器轮询？是作为兜底降级方案吗？
    *   **追问：** Flutter 中的 Isolate（隔离区）有用在处理繁重的地图计算或数据解析上吗？

### 5. 采购中心H5移动端 (Vue3 + AlovaJS + Vant)
**考察点：** 新技术选型、移动端适配

*   **问题一：** 我注意到你使用了 `AlovaJS` 进行网络请求，而不是常见的 Axios。请问出于什么考虑选择了 AlovaJS？它在缓存策略或请求压缩方面有什么特别优势吗？
*   **问题二：** 项目内置于企业微信。在处理企业微信的 OAuth2 授权登录流程中，前端是如何配合后端保证 Token 安全性和无感刷新的？

### 6. 小紫平台web端 (Vue2 + Threejs + Large File)
**考察点：** 大文件处理、可视化、Web Worker

*   **问题一：** 关于“分片进行大文件上传”和“WebWorker进行大文件下载”，请详细描述一下分片上传中断点续传的实现逻辑？文件 Hash 是如何计算的？
    *   **追问：** 在 Web Worker 中处理大文件流时，如何与主线程通信？是否遇到过内存泄漏的问题？
*   **问题二：** 结合 Three.js 和 ECharts 创建大屏展示时，如何解决 3D 模型加载过慢导致页面卡顿的问题？有没有做模型压缩或 LOD（多细节层次）处理？

### 7. 基础组件库 (Vue3 + Vitepress - 2024年段)
**考察点：** 文档建设、样式处理

*   **问题一：** 这段经历与前面的组件库经历类似。请问在这次“定制化一套针对公司风格的基础组件库”时，在样式覆盖（Theming）方面采用了什么方案？（CSS Variables / Sass Mixins / Design Tokens）？
*   **问题二：** 提取业务中高复用的组件时，如何界定一个组件是属于“基础组件”还是“业务组件”？

### 8. 1械网电商平台系列 (商家端/买家PC/买家移动端)
**考察点：** 电商业务逻辑、Vue2/3迁移、SKU算法

*   **问题一：** 在买家PC端提到“封装了Sku组件，通过有效路径字典进行规格过滤”。请问你是如何实现 SKU 算法的？是使用了邻接矩阵还是其他算法来判断哪些规格组合是不可选（置灰）的？
*   **问题二：** 移动端采用了 Uniapp，PC端采用了 Vue2。在多端开发中，有没有复用公共的业务逻辑代码（如 API 层、Utils 层）？
*   **问题三：** 图片懒加载采用了“可视区渲染策略”。请问是使用了 `IntersectionObserver` API 还是监听 `scroll` 事件？在快速滚动场景下如何做节流处理？

### 9. 医修保微信小程序工程师端 (Taro + NutUI + Canvas)
**考察点：** 小程序特有能力、Canvas交互

*   **问题一：** 关于“工单确认签名的 canvas 实现”，在不同分辨率的手机上，如何保证签名的笔触流畅且清晰度一致？最终生成的图片是如何上传给后端的（Base64 还是 Blob）？
*   **问题二：** 使用 `crypto-js` 加密数据，是在网络传输层加密还是对敏感字段（如用户信息）进行加密？前端保存密钥安全吗？

### 10. 早期项目 (农发证、农产品追溯、嵌入式相关)
**考察点：** 软硬结合、历史技术栈

*   **问题一：** (针对农发证项目) 提到“与蓝牙打印机通信”。在 Web/H5 环境下是如何连接蓝牙设备的？（Web Bluetooth API 还是通过 App 壳子桥接？）
*   **问题二：** 你的背景是从嵌入式软件转到前端开发的。你觉得嵌入式开发中对内存管理、底层驱动的理解，对你现在做前端性能优化有什么独特的帮助吗？

---

### 综合素质评估（可选提问）
*   **技术视野：** 你在简历中体现了 Vue2, Vue3, React, Flutter 多种技术栈。在未来的一两年内，你个人更看好哪种技术方案在企业级应用中的发展？为什么？
*   **团队协作：** 简历多次提到“制定前端开发规范”和“提升团队效率”。除了代码规范（ESLint），你在团队协作流程（Code Review, Git Flow）上有什么具体的推行经验？