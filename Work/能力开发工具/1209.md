总体评价
1 姜金晶 12年还是调库，写页面够用，对业务和技术没有自己的思考 看薪资要求决定
2 夏汉斌 pass
3 张文武 主要是干外包，倾向于pass
4 薛文瑜 学历ok，思路比较清晰，技术底层能讲出来一些，讲话方面似乎有一些小问题？
# 1 姜金晶

### 1. 享坐车中台管理-客户端 (React + TS + AntD)
**考察点：** React性能优化、权限设计、复杂组件封装

*   **问题一：** 简历中提到使用了 `HOC` (高阶组件) 实现了按钮级别的权限控制。请具体讲讲你的实现思路？是如何配合后端接口数据来控制组件渲染的？
    *   **追问：** 如果权限数据量很大，或者需要动态变更权限，这套方案如何保证页面渲染性能？
*   **问题二：** 关于“独立封装Excel导出插件”，是在纯前端完成的吗？如果是，使用了什么库（如 SheetJS）？在处理大数据量（例如几万条数据）导出时，如何避免浏览器卡顿或崩溃？
    *   **追问：** 有没有考虑过使用 Web Worker 来处理耗时的导出计算？
*   **问题三：** 你提到了使用 `useMemo` 和 `React.memo` 优化性能。在实际开发中，你是如何判断一个组件或计算逻辑需要被缓存的？有没有遇到过过度优化导致的问题？

### 2. 天猫校园三亚学院摆渡车 (Uniapp + Vue3 + Websocket)
**考察点：** 实时通信、地图交互、移动端性能

*   **问题一：** 项目中使用了 WebSocket 实现车辆呼叫和位置上传。请问你是如何处理 WebSocket 的断线重连（Heartbeat/Reconnect）机制的？在弱网环境下（如校园死角）表现如何？
    *   **追问：** 当消息并发量较大时，前端如何处理消息堆积或乱序问题？
*   **问题二：** 在高德地图上实现“车辆运行线路绘制及动态描点”，由于车辆位置是离散的点，前端是如何实现车辆平滑移动效果的？
    *   **追问：** 如果司机端上传的 GPS 坐标有漂移，前端做了哪些纠偏处理？

### 3. PC端基础组件库 (Vue3 + Monorepo + Verdaccio)
**考察点：** 工程化、架构设计、组件库建设

*   **问题一：** 你使用了 `pnpm` + `Monorepo` 进行管理。相比于 npm 或 yarn，pnpm 在这个场景下解决了什么核心问题？
    *   **追问：** 在 Monorepo 架构下，如何处理不同子包（package）之间的版本依赖和循环引用问题？
*   **问题二：** 对 ElementPlus 进行二次封装时，主要封装了哪些类型的业务逻辑？如何保持封装后的组件依然具有较高的灵活性（比如透传 Props 和 Slots）？
    *   **追问：** 使用 Verdaccio 搭建私有库时，有没有配置权限管理？CI/CD 流程是如何实现自动发包的？

### 4. 享坐车-车机屏司机端 (Flutter + Websocket)
**考察点：** 跨平台开发、原生交互、性能调优

*   **问题一：** 简历提到“优化启动白屏”。请问你是通过哪些具体手段解决 Flutter 启动白屏问题的？（例如：原生启动图、引擎预热等）
*   **问题二：** 这里提到“使用定时器对 Websocket 请求进行定时轮询”。这看起来有点矛盾，通常 Websocket 是全双工通信，为什么要结合定时器轮询？是作为兜底降级方案吗？
    *   **追问：** Flutter 中的 Isolate（隔离区）有用在处理繁重的地图计算或数据解析上吗？

### 5. 采购中心H5移动端 (Vue3 + AlovaJS + Vant)
**考察点：** 新技术选型、移动端适配

*   **问题一：** 我注意到你使用了 `AlovaJS` 进行网络请求，而不是常见的 Axios。请问出于什么考虑选择了 AlovaJS？它在缓存策略或请求压缩方面有什么特别优势吗？
*   **问题二：** 项目内置于企业微信。在处理企业微信的 OAuth2 授权登录流程中，前端是如何配合后端保证 Token 安全性和无感刷新的？

### 6. 小紫平台web端 (Vue2 + Threejs + Large File)
**考察点：** 大文件处理、可视化、Web Worker

*   **问题一：** 关于“分片进行大文件上传”和“WebWorker进行大文件下载”，请详细描述一下分片上传中断点续传的实现逻辑？文件 Hash 是如何计算的？
    *   **追问：** 在 Web Worker 中处理大文件流时，如何与主线程通信？是否遇到过内存泄漏的问题？
*   **问题二：** 结合 Three.js 和 ECharts 创建大屏展示时，如何解决 3D 模型加载过慢导致页面卡顿的问题？有没有做模型压缩或 LOD（多细节层次）处理？

### 7. 基础组件库 (Vue3 + Vitepress - 2024年段)
**考察点：** 文档建设、样式处理

*   **问题一：** 这段经历与前面的组件库经历类似。请问在这次“定制化一套针对公司风格的基础组件库”时，在样式覆盖（Theming）方面采用了什么方案？（CSS Variables / Sass Mixins / Design Tokens）？
*   **问题二：** 提取业务中高复用的组件时，如何界定一个组件是属于“基础组件”还是“业务组件”？

### 8. 1械网电商平台系列 (商家端/买家PC/买家移动端)
**考察点：** 电商业务逻辑、Vue2/3迁移、SKU算法

*   **问题一：** 在买家PC端提到“封装了Sku组件，通过有效路径字典进行规格过滤”。请问你是如何实现 SKU 算法的？是使用了邻接矩阵还是其他算法来判断哪些规格组合是不可选（置灰）的？
*   **问题二：** 移动端采用了 Uniapp，PC端采用了 Vue2。在多端开发中，有没有复用公共的业务逻辑代码（如 API 层、Utils 层）？
*   **问题三：** 图片懒加载采用了“可视区渲染策略”。请问是使用了 `IntersectionObserver` API 还是监听 `scroll` 事件？在快速滚动场景下如何做节流处理？

### 9. 医修保微信小程序工程师端 (Taro + NutUI + Canvas)
**考察点：** 小程序特有能力、Canvas交互

*   **问题一：** 关于“工单确认签名的 canvas 实现”，在不同分辨率的手机上，如何保证签名的笔触流畅且清晰度一致？最终生成的图片是如何上传给后端的（Base64 还是 Blob）？
*   **问题二：** 使用 `crypto-js` 加密数据，是在网络传输层加密还是对敏感字段（如用户信息）进行加密？前端保存密钥安全吗？

### 10. 早期项目 (农发证、农产品追溯、嵌入式相关)
**考察点：** 软硬结合、历史技术栈

*   **问题一：** (针对农发证项目) 提到“与蓝牙打印机通信”。在 Web/H5 环境下是如何连接蓝牙设备的？（Web Bluetooth API 还是通过 App 壳子桥接？）
*   **问题二：** 你的背景是从嵌入式软件转到前端开发的。你觉得嵌入式开发中对内存管理、底层驱动的理解，对你现在做前端性能优化有什么独特的帮助吗？

---

### 综合素质评估（可选提问）
*   **技术视野：** 你在简历中体现了 Vue2, Vue3, React, Flutter 多种技术栈。在未来的一两年内，你个人更看好哪种技术方案在企业级应用中的发展？为什么？
*   **团队协作：** 简历多次提到“制定前端开发规范”和“提升团队效率”。除了代码规范（ESLint），你在团队协作流程（Code Review, Git Flow）上有什么具体的推行经验？

# 2 夏汉斌
### 1. 项目：天秤平台+播主后台 (2024.09 - 2025.12)
**考察点：** WebSocket 稳定性、长列表性能优化、大文件处理

*   **问题一：** 你在项目中提到 WebSocket 连接稳定性达到 99.8%，并实现了心跳机制。
    *   **追问：** 请具体描述你的心跳检测逻辑（是前端主动 ping 还是后端主动 ping）？如果网络断开，你的重连策略是怎样的（例如：指数退避算法）？如何避免在弱网下频繁重连导致的服务器压力？
*   **问题二：** 关于“虚拟滚动优化评论渲染”，你说短时间推送 1000+ 条评论能保持 60fps。
    *   **追问：** 这里的虚拟滚动是手写的还是使用了第三方库（如 `vue-virtual-scroller`）？如果评论内容的高度是不固定的（有的长有的短），你是如何计算滚动偏移量和渲染列表的？
*   **问题三：** 简历提到“利用 MD5 计算哈希值进行大文件切片上传”。
    *   **追问：** 计算大文件（例如 1GB 以上）的 MD5 非常耗时，如果在主线程计算会导致页面卡顿。你是如何解决这个问题的？（期待回答：Web Worker）。切片上传中，如果中间某一片上传失败，你的重试机制是怎样的？

### 2. 项目：弗兰德数字员工平台 (2023.12 - 2024.06)
**考察点：** 工程化、身份认证、Canvas 交互

*   **问题一：** 你提到了使用 **Monorepo** 管理代码。
    *   **追问：** 你使用了什么工具来实现 Monorepo（pnpm workspace, Lerna, Turborepo）？在 Monorepo 架构下，不同子项目之间的依赖版本冲突是如何解决的？公共组件库的构建和热更新体验如何？
*   **问题二：** 关于“双 Token（Refresh + Access）无感刷新”功能。
    *   **追问：** 这是一个经典场景。如果 Access Token 失效，同时有 5 个并发请求发出去，如何保证只发送 **一次** Refresh Token 请求，并在刷新成功后重发这 5 个请求？（考察 Promise 控制和请求队列）。
*   **问题三：** 项目中利用 **Canvas** 实现票据 OCR 识别结果的展示。
    *   **追问：** 这里的 Canvas 具体做了什么？是仅仅绘制边框吗？如果图片的显示尺寸和原始分辨率不一致，你是如何进行坐标换算，确保框选位置准确的？

### 3. 项目：医疗救助面审即享系统 (2023.06 - 2023.11)
**考察点：** 前端安全、加密算法、CICD

*   **问题一：** 你提到将登录请求从 AES 加密替换为 **RSA 加密**。
    *   **追问：** 为什么要这么做？在前端场景下，RSA 的公钥和私钥是如何管理的？你是用公钥加密还是私钥加密？（正确应为：后端给公钥，前端加密，后端私钥解密）。
*   **问题二：** 配合后端使用 Jenkins 实现 CICD 自动化部署。
    *   **追问：** 你是否参与了 Jenkins Pipeline 的脚本编写（Jenkinsfile）？前端构建产物（Dist）是如何传输到目标服务器的（rsync, scp, docker push）？
*   **问题三：** 关于“通过 DOM 模拟超链接点击事件实现 Excel 导出”。
    *   **追问：** 这种方式和直接使用 `window.open` 有什么区别？如果后端返回的是二进制流（Blob），前端在处理文件名乱码问题时遇到过坑吗？

### 4. 项目：飞扬安全实验室系统 (2022.12 - 2023.06)
**考察点：** 权限控制、动态路由、Axios 封装

*   **问题一：** 简历中提到“生成动态的多级侧边栏菜单”和“动态路由”。
    *   **追问：** 后端返回的权限数据结构是怎样的？前端在使用 `router.addRoute` 时，如何处理 404 页面的路由顺序问题？（如果 404 路由注册过早，可能导致动态路由无法访问）。
*   **问题二：** 关于 Axios 二次封装和 CSRF 防御。
    *   **追问：** 你设置了 `csrfuuid`。请问这与浏览器原生的 `SameSite` Cookie 属性有什么区别？如果发生跨域请求，这个自定义 Header 能带过去吗？需要配合做什么配置？

### 5. 项目：大橘健身预约小程序 (2022.03 - 2022.05)
**考察点：** 小程序原理、性能优化

*   **问题一：** 你提到了小程序“分包处理”以解决 2M 限制。
    *   **追问：** 你是如何规划分包策略的？（按业务模块拆分 vs 独立分包）。主包和分包之间的资源引用有什么限制？
*   **问题二：** 支付功能实现。
    *   **追问：** 小程序支付流程中，前端需要做哪些核心操作？支付状态的最终确认是以通过前端回调为准，还是以轮询后端接口为准？为什么？

---

### 综合素质评估（可选提问）

*   **基础知识：** 你简历中写了熟练掌握 Vue2 和 Vue3。请简述一下 Vue3 的 `Proxy` 相比 Vue2 的 `Object.defineProperty` 在响应式实现上的核心优势是什么？（考察：数组监听、属性添加/删除）。
*   **学习能力：** 作为一个非计算机专业的毕业生（网络与新媒体），你是如何系统学习前端知识的？最近在关注哪些前端前沿技术？
*   **团队协作：** 简历提到“与后端开发团队配合，保证接口联调无误”。如果后端接口文档更新滞后，或者接口返回数据结构经常变动，你会如何推动解决这个问题？

# 3 张文武
## 1. AI 故事机（2025.09–2025.12）

> 小程序端声纹录制、AI 表情、新闻热点速览；总后台智能体配置、设备列表、订阅记录、资源监控；租户后台故事管理、兑换码管理、用户列表等

**Q1：小程序端声纹录制这个功能，从前端角度你是怎么实现的？整个数据流大致是怎样的？**

- 若对方回答了调用录音能力、上传 API：
    
    - 追问1：录音过程中，你是怎么处理录制时长、用户取消、网络中断等异常场景的？
        
    - 追问2：声纹录制通常对音质有要求，你在前端有没有做过采样率、音频格式（如 PCM/WAV）相关的处理或转换？
        
- 若回答比较抽象只说“调接口、写页面”：
    
    - 追问3：那在实现过程中，哪个具体问题是你印象最深的？你怎么一步步排查并解决的？
        

**Q2：项目里涉及平台总后台、租户后台和 C 端小程序，多端共用一些配置（比如智能体配置、故事内容）。你们是怎么设计这一块的配置和数据结构，让三端都能用？**

- 若能说到统一接口 / 配置中心 / 多端共用字段：
    
    - 追问1：前端在接这些通用配置时，你有做过类型约束或封装吗？比如用 TypeScript type/interface、封装请求层？
        
    - 追问2：当租户做个性化配置时，前端怎么兼容“公共配置 + 租户自定义配置”的合并和优先级？
        
- 若回答比较笼统：
    
    - 追问3：你举一个实际的配置（比如故事推荐规则或兑换码策略），说说这个配置从后台到小程序的流转过程。
        

**Q3：新闻热点速览这块，涉及到列表数据的更新和展示。你们有没有考虑过缓存、下拉刷新、分页等体验问题？你具体是怎么做的？**

- 若能提到分页 / 上拉加载 / 缓存策略：
    
    - 追问1：在网络较差或接口慢的情况下，你们是怎么避免重复请求或“抖动”的？
        
    - 追问2：如果后台的热点数据更新频率较高，你如何在前端兼顾“实时性”和“避免频繁刷新打断用户阅读”？
        
- 若回答很浅：
    
    - 追问3：假设你现在要优化“热点列表偶尔刷出空白页面”的问题，你会从哪些方面入手排查？
        

---

## 2. 里享商业（社区团购平台，2025.07–2025.09）

> 瓷片区后台管理拖动排序、团长端 APP 货源管理 / 订单列表 / 订单核销等

**Q1：你在瓷片区后台管理中实现拖动排序，这一块的技术方案是怎样的？如何保证拖动顺序能稳定同步到小程序首页？**

- 若能讲到拖拽库 / 自己实现 drag 事件 / 后端排序字段：
    
    - 追问1：前端拖拽结束后，排序信息是如何持久化的？比如用 sortIndex 字段还是其他方式？
        
    - 追问2：当多个运营人员同时在线调整排序时，你们有处理并发或覆盖的问题吗？
        
- 若回答比较表层：
    
    - 追问3：假设拖动操作失败但 UI 排序已经改变，你会怎么保证前端 UI 状态与后端真实排序的一致性？
        

**Q2：团长端 APP 的订单列表和订单核销，通常会有高并发和网络不稳定的问题。你们是怎么处理加载性能和核销的“幂等性”的？**

- 若能提到分页、懒加载、本地缓存、幂等接口：
    
    - 追问1：订单核销时，如果用户点了多次“核销”按钮，你在前端做了哪些防重复提交的措施？
        
    - 追问2：在离线或弱网场景下，你认为前端可以做哪些优化来保证团长的基本操作不受太大影响？
        
- 若回答不涉及幂等：
    
    - 追问3：如果后端没有专门做幂等，你在前端会怎样降低重复请求带来的问题？
        

**Q3：C 端用户小程序涉及下单、查看物流、申请退款等完整链路。你参与的页面中，哪个环节的体验优化是你主导的？你具体做了什么？**

- 若能说到某个具体优化（比如加载占位、表单校验、错误提示）：
    
    - 追问1：这个优化落地后，有没有通过埋点数据或反馈来验证效果？
        
    - 追问2：如果要再迭代一次，你觉得还能怎么做得更好？
        
- 若回答“照产品设计开发”：
    
    - 追问3：那你遇到过用户反馈的问题吗？你当时是如何排查并和产品沟通改动的？
        

---

## 3. 3D 魔方（2025.05–2025.06）

> Three.js + TypeScript，渲染魔方、执行还原公式动画

**Q1：在 3D 魔方项目里，你是如何用 Three.js 和 TypeScript 组织魔方的数据结构和场景渲染的？**

- 若能讲到 cube -> 小立方体 / mesh / group、场景 / 摄像机 / 渲染循环：
    
    - 追问1：魔方每个小块的状态（颜色、位置、朝向）是如何与安卓传入的“面状态数据”对应起来的？
        
    - 追问2：你在 TypeScript 里有做类型定义吗？比如对面状态、旋转步骤的类型封装？
        
- 若回答停留在“用 three.js 画个立方体”：
    
    - 追问3：假设现在要支持 4x4 或 5x5 魔方，你会从哪些代码结构入手重构，避免写死？
        

**Q2：对于还原公式的单步执行和动画，你具体是怎么设计的？如何保证动画流畅而且不会乱序？**

- 若能说到动画队列 / tween / requestAnimationFrame：
    
    - 追问1：如果用户在动画执行中途希望暂停或者加快播放速度，你会怎样设计数据结构和状态管理？
        
    - 追问2：在低端 Android 机上遇到过性能问题吗？你做过什么优化？
        
- 若回答“调用接口返回结果然后直接更新状态”：
    
    - 追问3：那转动动画是如何实现的？你能具体说说一帧里发生了什么？
        

---

## 4. 番茄免费短剧（抖音小程序，2025.04–2025.05）

> UniappX，小程序登录、追剧记录、广告对接、埋点等

**Q1：你在短剧小程序里，有做哪些和“用户观看记录 / 追剧记录”相关的逻辑？数据是如何在前端维护和上报的？**

- 若能讲到本地存储 / 分页上报 / 防止丢数据：
    
    - 追问1：用户在多个设备上观看，你们是如何保证观影进度同步的？
        
    - 追问2：如果接口失败，前端的追剧记录怎么做重试或补偿？
        
- 若回答比较抽象：
    
    - 追问3：举一个具体场景：用户看完第 5 集退出再进来，你的代码如何保证他回到第 6 集或继续第 5 集观看？
        

**Q2：你提到对接抖音官方广告渠道和埋点，这里面从前端角度主要做了哪些工作？**

- 若能说到广告位配置、曝光 / 点击埋点、事件上报：
    
    - 追问1：埋点逻辑是怎样避免重复上报或遗漏的？有没有做过防抖 / 节流？
        
    - 追问2：广告加载失败或被用户关闭时，你们的 UI 如何降级处理？
        
- 若回答只说“按照文档接了几个方法”：
    
    - 追问3：那你遇到过文档不清晰或 SDK 有 bug 的情况吗？你是怎么定位和解决的？
        

---

## 5. M-AI 桌面客户端（Electron，2023.08–2024.09）

> 跨平台 AI 工具：AI 写作 v1/v2/v3、WPS WebOffice、tiptap 富文本、文稿校对、差异高亮等

**Q1：AI 写作模块经历了 v1 / v2 / v3 多个版本迭代，你参与最多的是哪一版？你能具体讲一下这版的技术方案和关键改动吗？**

- 若能系统讲清：比如 v1 WangEditor + selection & range，v2 Vditor + WebOffice，v3 tiptap 深度定制：
    
    - 追问1：在这几版迭代中，最核心的技术痛点是什么？你们是如何一步步拆解并解决的？
        
    - 追问2：如果现在再做一次，你会从一开始就如何设计整体架构，避免后面的重构成本？
        
- 若回答偏履历式：
    
    - 追问3：挑一个你亲手写的功能，例如“差异高亮”或“模板预览”，从接口、状态管理到 UI 渲染，详细走一遍流程。
        

**Q2：文稿校对需要展示 AI 校对前后的差异高亮，并支持逐条接受 / 拒绝。你是如何在数据结构和 UI 层面设计这套交互的？**

- 若能提到 diff 结构（按段落 / 按 token）、唯一 ID、操作历史栈：
    
    - 追问1：用户连续撤销 / 重做时，你们是怎么保证文档状态与操作历史一致的？
        
    - 追问2：在长文档情况下，差异高亮对性能有什么影响？你做过哪些优化？
        
- 若回答很浅：
    
    - 追问3：假设现在出现“部分差异高亮消失 / 错位”的 bug，你会按什么顺序排查？
        

**Q3：你们深度定制了 tiptap 和 WPS WebOffice，这里面最难的一块集成是哪个？你具体做了哪些适配？**

- 若能讲到 schema 扩展、command / extension、自定义 toolbar、与 WebOffice 模板互相转换：
    
    - 追问1：tiptap 内部是基于 ProseMirror，你有接触其 transaction / state / node schema 吗？
        
    - 追问2：在 Electron 环境下对 WebOffice 进行预览和导出时，有没有遇到安全或跨进程（主进程 / 渲染进程）的问题？
        
- 若回答“按文档集成，没有太多定制”：
    
    - 追问3：那你举一个你写的扩展点，比如自定义段前段后、行间距或序号列表，是如何接入的？
        

---

## 6. M-AI 后台管理系统（2024.07–2024.09）

> AI 写作助手管理、动态表单配置、docx 模板管理、new Function 动态执行校验 JS

**Q1：你提到通过后台配置动态表单，并在前端通过 `new Function()` 执行校验和预处理逻辑。能详细说说整个链路设计吗？**

- 若能讲到：配置结构（字段 + 规则）、下发前端、运行校验函数：
    
    - 追问1：使用 `new Function()` 带来哪些安全风险？你们做过什么限制（如只读上下文、黑白名单、沙箱）？
        
    - 追问2：当某个动态脚本抛异常时，你们是如何降级处理并提示用户的？
        
- 若只停留在“执行一段 JS 代码”：
    
    - 追问3：如果让你重新设计，你会考虑用什么更安全 / 更可控的方案来实现“配置驱动的表单校验”？
        

**Q2：docx 模板文件管理上，后台如何与客户端的模板应用逻辑对齐？你负责的部分主要做了哪些工作？**

- 若能讲到模板元数据、版本号、关联写作场景、前端下载和缓存：
    
    - 追问1：当模板更新时，如何避免客户端使用旧模板导致内容错位？
        
    - 追问2：你们有没有做过“预览缩略图”的优化，比如懒加载或按需生成？
        
- 若回答比较泛：
    
    - 追问3：假设模板数量达到几百个，你觉得后台管理界面应该增加哪些功能来提升运营效率？
        

---

## 7. LIMS 实验室管理系统（2023.05–2023.07）

> Flowable 工作流、jsPDF 导出报告、echarts 可视化、公共组件封装

**Q1：在接入 Flowable 工作流做审批时，从前端视角你主要处理了哪些问题？**

- 若能说到：流程图展示、节点状态、表单与流程节点绑定：
    
    - 追问1：你们如何根据不同流程节点动态切换前端表单的可编辑 / 只读状态？
        
    - 追问2：当业务对流程提出修改（增加节点 / 条件）时，你们前端的改动成本大吗？为什么？
        
- 若回答只停留在“调了几个接口”：
    
    - 追问3：如果现在要在前端给用户展示“流程进度条”和“当前处理人”，你会怎么设计？
        

**Q2：你封装了查询弹窗、附件列表、流程提交等通用组件，这些组件在设计时是如何做到“可复用又不死板”的？**

- 若能讲到 props 设计、插槽、事件、自定义渲染：
    
    - 追问1：举一个组件，说明你为复用性考虑加了哪些 props 或插槽？
        
    - 追问2：这些组件在后续项目（比如 MES）里有被复用吗？在复用过程中有没有发现最初设计不合理的地方？
        
- 若回答泛泛：
    
    - 追问3：假如现在让你重写“查询弹窗组件”，你会怎么划分“通用部分”和“业务自定义部分”？
        

**Q3：实验数据导出 PDF 这一块，你用 jsPDF 做了什么定制？在导出过程中遇到过哪些实际问题？**

- 若能提到分页、表格宽度、自适应、图表导出：
    
    - 追问1：echarts 图表是如何嵌入到 PDF 中的？
        
    - 追问2：当数据量较大时，PDF 生成耗时和内存使用有没有遇到过瓶颈？你是怎么优化的？
        
- 若回答“一般没啥问题”：
    
    - 追问3：那你举一个导出版式错乱或中文字体问题的 bug，说说你最后是怎么解决的。
        

---

## 8. 芯缘 MES + EMS（Web + H5，2023.04–2023.06）

> RxJS、jsPlumb 工艺流程可视化、虚拟列表性能优化等

**Q1：你在这个项目中使用 jsPlumb 实现了工艺流程节点的可视化拖拽编辑，能具体说说前端是怎么组织节点 / 连接线的数据结构和渲染逻辑的吗？**

- 若能讲到节点配置、连线配置、拖拽回调、持久化：
    
    - 追问1：从“流程图界面”到“后端保存的流程配置”，你们做了怎样的转换和校验？
        
    - 追问2：当流程比较复杂、节点很多时，jsPlumb 的性能和操作体验有没有遇到问题，你如何优化？
        
- 若回答只是“用 jsPlumb 官方 demo 改的”：
    
    - 追问3：如果现在要支持流程的撤销 / 重做，你会在现有结构上怎么加？
        

**Q2：你提到对“大数据量列表做了虚拟列表优化”，大概是什么场景？你用了什么实现方案？**

- 若能说到自实现虚拟滚动 / 使用现成组件、可视区域计算、占位高度计算：
    
    - 追问1：在 H5 端和 PC 端的表现有没有差异？怎么避免在手机上滚动时“白屏闪烁”？
        
    - 追问2：虚拟列表和分页加载有什么配合需求？你们是如何找到一个平衡点的？
        
- 若回答“没做太多，只是简单分页”：
    
    - 追问3：那如果后台要求一屏展示上万条数据，你会如何说服他们改需求，或在技术上给出折中方案？
        

---

## 9. 爱康 MES（2022.11–2023.02）

> html2canvas、tiff.js、jszip 批量导出、跨域处理、Promise 控制并发和进度条等

**Q1：在 Mapping 图批量导出这块，你们是如何用 html2canvas + tiff.js + jszip 这几个库组合起来实现的？**

- 若能讲清：dom -> canvas -> tiff -> zip 打包：
    
    - 追问1：在批量导出时，有哪些地方需要控制并发？你是如何设计 Promise 并发控制的？
        
    - 追问2：导出过程中的进度条是基于什么信息更新的？是任务数、已完成数还是字节大小？
        
- 若回答比较泛：
    
    - 追问3：如果导出的 zip 文件过大导致浏览器卡死，你会有哪些优化思路？
        

**Q2：你提到 html2canvas 导出含图片元素时有跨域问题，最后通过 Nginx 反向代理解决。能说说这个问题的本质原因和你的解决步骤吗？**

- 若能说到 canvas 污染、CORS、代理到同源域名：
    
    - 追问1：如果不能改 Nginx 配置（例如 SaaS 场景），你在前端还会有哪些备选方案？
        
    - 追问2：在生产环境切换到代理后，你们如何验证没有引入新的性能或缓存问题？
        
- 若回答“不太记得，就按后端说的改了”：
    
    - 追问3：那你还记得当时调试时使用了哪些工具或手段吗？比如浏览器控制台、Network 面板、响应头检查等？
        

**Q3：这个项目中你做了“封装通用旋转缩放组件、处理不确定数量 TIFF 图片同一页面展示”，这块组件设计是怎样的？**

- 若能讲到：统一手势 / 鼠标事件、独立图片状态管理、同步 / 独立变换：
    
    - 追问1：当图片数量较多时，旋转缩放操作对性能有什么影响？你们如何避免频繁重绘？
        
    - 追问2：组件是否有后续复用到其他项目？复用时有没有发现最开始没考虑到的场景？
        
- 若回答不清晰：
    
    - 追问3：如果现在让你在 Web 页里做一个“无限缩放 + 拖动的 TIFF 查看器”，你会怎么选型和拆分组件？
        
# 4 薛
## 一、项目一：上海铁铁供应链系统（Vue3 + Pinia + TS + Element Plus + Vite）

> 关键词：动态表单引擎、二次封装组件、接口并发控制、打包优化、预加载插件

### Q1：你在“动态表单引擎”这块具体做了哪些设计？从 JSON 配置到实际渲染，中间经过了哪些步骤？

- 如果对方能讲出字段 schema / 类型、渲染引擎、校验机制等：
    
    - 追问1：表单项之间有依赖关系（比如 A 选了某个值才显示 B）时，你们是如何在配置层和渲染层处理这种联动逻辑的？
        
    - 追问2：动态表单经常要支持自定义组件（如上传、级联选择），你们是怎么在“通用表单引擎”中挂载和配置这些特殊组件的？
        
- 如果回答比较笼统，只说“根据 JSON 生成表单、提升效率”：
    
    - 追问3：你能举一个具体字段配置的例子吗？比如一个“选择+输入”的组合控件，它在 JSON 配置和实际渲染时分别长什么样？
        

---

### Q2：你提到“二次封装 element-plus 组件库，减少 CRUD 代码”，能详细说一下封装了哪些组件？封装时你是如何权衡通用性和灵活性的？

- 如果能说出封装表格、搜索表单、对话框、分页等：
    
    - 追问1：以列表+弹窗编辑为例，你封装的组件是如何支持不同业务字段的？用的是插槽、配置对象，还是别的方式？
        
    - 追问2：在后续业务迭代中，有没有遇到你最开始的封装反而限制了需求的情况？最终是怎么调整的？
        
- 如果回答停留在“封了一些通用组件，提高复用”：
    
    - 追问3：假设现在要新做一个“带树形筛选 + 联动表格”的页面，你会怎么基于现有封装来快速搭起来？请你口头过一遍开发步骤。
        

---

### Q3：关于“限制接口并发、br 压缩、拆包和基于 Vite 的预加载插件”，你觉得对性能提升最大的一个点是哪一个？你参与实现的细节是什么？

- 如果候选人能选出一个点并展开（比如并发控制或预加载）：
    
    - 追问1（并发控制方向）：你们限制并发是为了解决什么实际问题？是浏览器连接数限制、后端压力，还是避免同一资源重复请求？你是如何实现队列 / 信号量的？
        
    - 追问2（预加载方向）：你封装的 Vite 预加载插件大概做了什么？是基于路由分包+link rel="prefetch"/"preload" 还是别的机制？
        
- 如果回答比较泛，只说“做了优化，包更小，加载更快”：
    
    - 追问3：那在你实际做完这些优化后，有没有对比过首屏加载时间、bundle 体积等指标？如果让你现在用一次 `performance` 工具评估，你会具体怎么测？
        

---

## 二、项目二：废钢质检系统（Vue3 + Pinia + Element Plus + Vite）

> 关键词：RBAC 权限模型、动态路由、SSO 单点登录、摄像头视频流、异物检测预警与语音播报

### Q1：你在“基于 RBAC 的动态路由与菜单权限系统”中具体实现了什么？从登录成功到生成侧边菜单，大致流程是怎样的？

- 如果能讲出权限数据结构（role、permission、menu）、前端动态路由、按钮级权限控制：
    
    - 追问1：按钮级权限你们是怎么做的？是通过自定义指令、组件封装，还是在渲染时判断？
        
    - 追问2：当后端新增一个菜单或权限点时，前端如何在不改代码或少改代码的前提下适配？
        
- 如果只说“根据后端返回的菜单动态生成路由”：
    
    - 追问3：那如果用户直接访问一个没有权限的路由（比如手动改 URL），你们前端是怎么处理的？重定向、提示、还是白屏？
        

---

### Q2：集成统一身份认证平台（SSO 单点登录）这块，你从前端视角做了哪些工作？你对整个认证流程的理解是怎样的？

- 如果能描述出重定向到认证中心、回调携带 token / code、前端存储和刷新流程：
    
    - 追问1：你们是如何处理 token 过期的？是静默刷新还是直接跳转登录？前端做过哪些拦截和错误处理？
        
    - 追问2：在多系统之间跳转（A 系统 → B 系统）时，如果 B 还没登录，你们是依靠 SSO 自动登录还是需要用户感知？
        
- 如果回答偏“按照文档接了登录接口”：
    
    - 追问3：假设现在你要在浏览器控制台里排查一个“偶发性 401 未授权”的问题，你会看哪些请求和哪些字段来判断是前端问题还是 SSO 配置问题？
        

---

### Q3：你们对接了宇视摄像头的视频流，并在异物识别后做实时预警和语音播报，这块你具体负责了哪些实现？前端是怎么拿到实时图像/事件的？

- 如果能说出 RTSP/RTMP 转 Web 方案、WebSocket / SSE 推送识别结果、告警 UI 和音频播放逻辑：
    
    - 追问1：实时视频和告警消息是两条通路还是一条？在 UI 上如何保证“画面看到的车”和“告警信息”对得上？
        
    - 追问2：语音播报是如何实现的？是用浏览器的 Web Speech API、Audio 标签播放预录音，还是其他方案？
        
- 如果回答比较泛，只说“能看到摄像头画面，有告警弹窗”：
    
    - 追问3：如果现场网络不好导致视频有延时或者卡顿，你会从哪里开始优化？是前端播放参数、流协议切换，还是只做 UI 层面的处理？
        

---

## 三、项目三：中电金信 妙如 X20 系统（React + Redux + TS + ECharts + Webpack）

> 关键词：医疗设备监控大屏、ECharts 图表与联动、轮询实时刷新、按需引入、二次封装、打包优化

### Q1：你在这个监控大屏项目里，具体负责了哪些图表和交互？ECharts 图表联动是怎么设计和实现的？

- 如果能说出：多图联动（如时间轴同步、点击某图高亮其他图）、数据结构设计、事件监听：
    
    - 追问1：当用户在某张图上进行缩放或筛选时，你们是如何把这个状态同步给其他相关图表的？是在 Redux 里维护，还是由某个统一的事件总线？
        
    - 追问2：面对几十个图表同时存在的情况，你们有没有遇到渲染性能问题？你做过哪些优化（懒加载、节流、分批渲染等）？
        
- 如果回答只说“用 ECharts 画了几个折线图、饼图”：
    
    - 追问3：假设现在要实现一个“点击某个设备卡片，右侧所有图表自动聚焦该设备的数据”的联动，你会怎么设计数据结构和交互流程？
        

---

### Q2：你们通过轮询实现图表数据的实时刷新，在轮询和性能之间是如何做权衡和设计的？

- 如果能说出轮询间隔设置、统一轮询中心、请求合并、防止重复请求：
    
    - 追问1：当用户切换页面或页面隐藏（比如切到其他 Tab），你们是如何处理轮询的？是否暂停 / 降频？
        
    - 追问2：在实际项目中，有没有通过埋点或监控发现轮询带来的问题（例如接口压力、CPU 占用较高）？后来是如何优化的？
        
- 如果回答比较简单，只说“定时器请求接口更新图表”：
    
    - 追问3：如果现在后端提出要把轮询改成 WebSocket 方式推送，你认为前端架构上需要改哪些地方？哪些模块可以复用？
        

---

### Q3：在 webpack 打包优化这一块（压缩、拆包、tree-shaking、按需引入 ECharts），你自己亲手改过哪些配置？最后效果有量化过吗？

- 如果能讲出具体配置项、splitChunks、externals、babel/ts-loader 优化等：
    
    - 追问1：ECharts 按需引入是如何做的？是手动引入单个图表和组件，还是用插件辅助？
        
    - 追问2：打包拆分之后，你们如何确认首屏加载和后续路由切换的体验确实提升了？用过哪些工具（如 webpack-bundle-analyzer、浏览器 performance）？
        
- 如果回答“按网上的教程配了下 tree-shaking 和压缩，没有深挖”：
    
    - 追问3：如果你接手一个首屏资源 5MB 的 React 项目，你会按照什么顺序一步步排查和优化？请你给一个行动列表。
        

---

如果后面你还有其他候选人的简历，可以继续发上来，我可以按同样格式：**按项目 → 主问题 → 细/泛两种回答的追问**，帮你整理一整套技术面试话术，方便你面试时直接挑选使用或灵活组合。

# 5 杜望
## 一、会轻松 App（Android + iOS 双端）

> 关键词：架构重构、蓝牙通信协议、弱网重试、Web Bluetooth / Web Audio、LLM 能力

### Q1：你说对会轻松 App 做了“应用架构重构”，减少约 30% 冗余代码、构建速度提升 25%。你具体重构了哪些模块？整体架构从什么样变成了什么样？

- **若回答比较具体**（提到状态管理、模块拆分、依赖反转等）：
    
    - 追问1：在迁移「原有核心功能」到新架构的过程中，你是如何控制风险的？有没有设计灰度或开关机制？
        
    - 追问2：举一个你认为“之前耦合很重，现在变得清晰”的模块，详细说一下你的拆分思路和边界划分。
        
- **若回答比较泛**（只说“重构、更清晰、可维护”）：
    
    - 追问3：假设现在要再新增一个功能模块，比如“睡眠分析”，在你重构后的架构中，从新建到上线会经过哪些步骤？
        

---

### Q2：项目里你集成了自定义蓝牙通信协议，通过 Web Bluetooth / BLE 分包传输并做校验优化。前端这块完整的数据流是怎么样的？

- **若能讲到分包、重传、CRC/校验、状态机**：
    
    - 追问1：在分包传输时，是前端负责切包合包，还是设备端？你们如何处理丢包和乱序的情况？
        
    - 追问2：你提到“延迟降低 35%”，这个数字是怎么来的？是通过埋点统计、设备日志，还是手工测量？
        
- **若回答停留在“调用蓝牙库、收发数据”**：
    
    - 追问3：如果现在现场反馈“偶发性蓝牙断连且数据不完整”，你会怎么定位是硬件问题、系统蓝牙栈问题还是你们协议设计问题？排查步骤说一说。
        

---

### Q3：这个 App 里还集成了大语言模型 API、语音识别和 Web Audio 的实时音频处理。你是怎么把这几个能力串在一起做一个完整的交互闭环的？

- **若能讲出：录音 → 语音识别 → LLM 请求 → 流式返回 → TTS / 文本展示**：
    
    - 追问1：在弱网环境中，你提到做了“网络异常重试机制”，在 LLM 调用这种有状态的场景下，如何避免重复提问 / 重复扣费？
        
    - 追问2：做情感识别和个性化健康建议时，你们前端有没有做过 prompt 组装或上下文管理？具体怎么设计的？
        
- **若回答偏泛，只说“接了语音和大模型接口”**：
    
    - 追问3：假设现在 LLM 接口偶尔超时，你会选择在前端增加排队提示、取消功能还是 silent retry？你会怎么权衡体验和成本？
        

---

## 二、Meerkat AI 平台（Web 端）

> 关键词：monorepo、SSE 流式输出、Web Worker、知识库 & 聊天记忆、AIGC 报告组件、Three.js、Sentry

### Q1：你们用 monorepo 管理前端工程，并抽离通用组件和工具库。能具体说说 monorepo 的目录结构、包划分方式，以及是如何做版本管理和依赖协调的吗？

- **若能说出 pnpm/Turbo/Lerna 等、包边界、发布流程**：
    
    - 追问1：在多应用（平台前台 + 后台）共用组件时，如何避免“随手改共享包导致另一端挂掉”？你们用的策略是什么？
        
    - 追问2：你在 monorepo 中遇到过最棘手的依赖冲突或循环依赖问题是什么？怎么解决的？
        
- **若回答比较泛，只说“方便复用组件、统一管理”**：
    
    - 追问3：假设我要在这个 monorepo 里再加一个“Web IDE 子应用”，你会怎么把它接入现有 monorepo（脚手架、CI、发布）？
        

---

### Q2：你负责用 SSE 实现大模型内容流式接收，并结合打字机动画渲染 Markdown 报告。整个渲染链路你是怎么设计的？重点是哪里？

- **若能说出：SSE 事件流 → 消息缓冲 → Markdown 渲染 → DOM 更新 / 虚拟列表**：
    
    - 追问1：长文档流式渲染时，你是怎么处理“频繁 setState / 重排”的性能问题？Web Worker 在这里具体承担了什么工作？
        
    - 追问2：如果流在中途异常中断，你们如何处理“半截内容”？是提示用户、允许继续补全，还是自动重连？
        
- **若回答停留在“按消息追加到页面”**：
    
    - 追问3：如果要在流式输出的同时支持“撤回这条回答”，你觉得前端在数据结构上要做哪些调整？
        

---

### Q3：平台里你还做了知识库挂载、聊天记忆和 AIGC 报告组件，这三块里你觉得哪一块最复杂？详细说说你在前端的设计和实现。

- **若能选一块展开（比如知识库 + 聊天记忆）**：
    
    - 追问1：为了让 Agent“记住上下文”，你们在前端如何管理会话上下文？是全部交给后端，还是在前端有缓存和裁剪策略？
        
    - 追问2：AIGC 报告组件支持模板规则套用，你们是用 JSON Schema / 配置驱动，还是直接写死模板？如果要支持“用户自定义模板”，你会怎么扩展？
        
- **若回答泛泛，只简单罗列功能**：
    
    - 追问3：假设现在让你为“工作流调度结果”做一个可视化报告组件，你会如何抽象输入数据结构和组件插槽，以便未来多场景复用？
        

---

## 三、Meerkat AI 后台管理平台

> 关键词：monorepo 共用模块、vue-flow Agent 编排器、模型接入 & 知识库管理、ECharts 监控、jsmind 工作流

### Q1：你用 vue-flow 封装了 Agent 流程编排器，支持拖拽、状态追踪和回溯。这个编排器在数据结构和组件拆分上是怎么设计的？

- **若能讲到：Node/Edge 数据结构、节点 schema、保存/回放**：
    
    - 追问1：一个 Agent 流程从画布上“连线”到后端真正可执行的工作流，中间做了哪些映射或校验？
        
    - 追问2：当业务团队要新增一种“节点类型”（比如 HTTP 调用、工具调用），前端需要改哪些地方？有没有做到配置化？
        
- **若回答泛，只说“拖拽连线、保存配置”**：
    
    - 追问3：如果要为这个编排器增加“调试回放”和“断点查看上下文”能力，你会怎么扩展现有结构？
        

---

### Q2：后台还负责模型接入、能力挂载、知识库管理等。你在“模型接入和版本管理”模块里具体做了哪些工作？

- **若能讲出参数配置表单、版本切换、在线测试面板**：
    
    - 追问1：你们是如何避免“误操作把生产模型改挂成测试版本”的？有做过权限控制或发布流程吗？
        
    - 追问2：在前端侧，你如何设计“同一 Agent 绑定多个模型版本”的展示和交互？
        
- **若回答泛，只说“配置模型参数、接接口”**：
    
    - 追问3：假如现在要支持多个云厂商模型（阿里、百度、OpenAI），你觉得前端的配置表单应该如何抽象，才能在不改大结构的前提下新增一个厂商？
        

---

### Q3：你提到平台中接入 ECharts 做调用监控、接入 jsmind 做脑图式工作流建模。你觉得在这些“复杂可视化组件”接入过程中，最关键的抽象点是什么？

- **若能说到：统一数据适配层、组件封装、事件总线**：
    
    - 追问1：例如 ECharts 大盘，你们是如何封装图表组件，使得“换一个指标/维度”时不需要大改代码？
        
    - 追问2：jsmind 的脑图与 vue-flow 的流程图在数据结构或交互上有什么共同点？你有尝试复用某些封装吗？
        
- **若回答比较泛**：
    
    - 追问3：如果你要把这些可视化能力迁移到一个新的项目（比如我们内部的管理平台），你会封装成什么样的组件 / npm 包？
        

---

## 四、悦途出行小程序（微信 + 支付宝，小程序端）

> 关键词：Taro + Zustand、多端编译、高德地图、支付、分包与预加载、埋点上报

### Q1：这个项目用了 Taro + Zustand 做多端小程序开发。你们的状态管理大致是怎么设计的？在多端兼容上踩过什么坑？

- **若能具体说出模块划分、store 分层、端差异处理**：
    
    - 追问1：微信和支付宝在 API 上有差异，你们是在封一层适配器，还是用条件编译？举个例子。
        
    - 追问2：Taro 升级或小程序基础库升级时，有没有遇到不兼容的问题？你是如何排查和回滚的？
        
- **若回答泛，只说“用 Taro 一套代码多端跑”**：
    
    - 追问3：假设现在要新增“抖音小程序端”，你认为在你们当前工程下需要改动的关键点有哪些？
        

---

### Q2：你负责高德地图、支付和埋点等功能模块。以“下单 + 派单 + 轨迹跟踪”为例，整条链路前端是怎么实现和监控的？

- **若能说清：下单表单 → 支付 → 轮询/推送订单状态 → 地图标注 + 轨迹**：
    
    - 追问1：在轨迹跟踪中，位置更新频率如何控制？你们有遇到过性能或电量方面的考量吗？
        
    - 追问2：埋点上报这块，有没有设计统一的埋点 SDK？事件命名、参数规范是怎么推进团队落地的？
        
- **若回答较浅，只分段介绍功能**：
    
    - 追问3：如果现在突然发现“支付完成率下降”，你会从前端数据角度怎么去排查问题？
        

---

## 五、悦途出行服务端 App（司机端 React Native）

> 关键词：React Native + Zustand、原生地图桥接、实时位置上传、二维码核销、证书与发布

### Q1：你用 RN + Zustand 做了司机端 App，并通过原生桥接集成高德导航。桥接层是怎么设计的？JS 与原生之间如何通信？

- **若能说出 NativeModule、事件通道、参数序列化等**：
    
    - 追问1：在导航过程中，原生侧的导航状态（如偏航、到达）如何通知到 JS，用于更新 UI 或触发埋点？
        
    - 追问2：不同平台（Android / iOS）在桥接实现上有区别吗？你是如何尽量让 JS 层逻辑保持一致的？
        
- **若回答泛，只说“原生同事写桥接，我调方法”**：
    
    - 追问3：假设现在导航偶发崩溃，但 React Native 日志很干净，你会如何协同原生同事排查？你会关注哪些信息？
        

---

### Q2：司机端有实时位置上传、二维码核销、异常上报等。你们是如何保证这些功能在弱网或离线场景下的可用性的？

- **若能讲到本地队列、重试机制、状态缓存**：
    
    - 追问1：位置上传的节流与批量上报策略怎么设计？如何兼顾实时性和流量/电量？
        
    - 追问2：二维码核销支持“异常跳过”时，你们如何保证核销记录的一致性和可追溯？
        
- **若回答泛，只说“调用接口失败就提示重试”**：
    
    - 追问3：如果你来重新设计离线策略，你会在哪些数据和操作上做本地持久化？
        

---

## 六、悦途礼宾车后台系统（React Admin）

> 关键词：React18 + Vite + AntD、工程规范链路、OCR、电子围栏、高级调度、AntV 可视化

### Q1：你说在这个项目中主导了工程规范（Eslint、Stylelint、Commitlint 等）。这些规范是如何嵌入到开发流程里的？有没有遇到团队落地阻力？

- **若能描述 pre-commit 钩子、CI 校验、Branch 策略等**：
    
    - 追问1：你用哪些方式让大家“愿意遵守”而不是“觉得很烦”？有做过自动修复、脚手架模板吗？
        
    - 追问2：在引入规范前后，你观察到的最明显的变化是什么？
        
- **若回答比较泛，只说“加了 lint，规范代码”**：
    
    - 追问3：如果一个新成员频繁因为 lint 过不了而抱怨，你会怎么帮助他？
        

---

### Q2：你实现了电子围栏、智能调度、结算分析等模块。这三个里你觉得哪一块最能体现你在前端侧的设计能力？

- **若选电子围栏**：
    
    - 追问1：围栏是如何配置和展示的？前端如何可视化编辑多边形区域并持久化给后端？
        
    - 追问2：当位置点频繁更新时，你如何保证地图组件的性能和交互流畅？
        
- **若选智能调度或结算分析**：
    
    - 追问1：调度规则在前端是如何展示和可视化的？有没有做模拟演算或“假设场景”展示？
        
    - 追问2：AntV 图表的封装有做到“指标可配置”吗？你是如何抽象图表组件的？
        

---

## 七、控江随身学小程序

> 关键词：uni-app、多端、游戏化学习、We 分析、分包 + 预加载、蓝牙打印

### Q1：你在这个项目中负责闯关/巩固模块和公共组件。作为一个面向学生的“游戏化”小程序，你在交互和性能上做了哪些特别的设计？

- **若能说到动效、进度反馈、跨页面状态、预加载等**：
    
    - 追问1：分包加载和预加载具体是如何配置的？你们根据什么规则拆包？
        
    - 追问2：We 分析的数据（比如完成率、流失点）有反过来指导你们改版页面吗？举一个例子。
        
- **若回答较泛**：
    
    - 追问3：如果现在发现“巩固练习完成率低、但进入人数不少”，你会从哪些埋点数据去分析原因？
        

---

### Q2：你实现了蓝牙链接打印机打印条形码。前端这块你做了哪些封装，来兼顾不同手机型号和打印机差异？

- **若能说到：设备扫描、配对、缓存、打印队列**：
    
    - 追问1：打印失败你们怎么做重试或错误提示？是否会记录失败任务以便老师重打？
        
    - 追问2：在 uni-app 这种多端框架下，蓝牙相关 API 有遇到兼容性问题吗？怎么绕过去的？
        
- **若回答泛**：
    
    - 追问3：如果我们要在项目里加一个“蓝牙硬件调试工具页”，你会放哪些信息和操作按钮，方便排查问题？
        

---

## 八、福利会幼儿园后台系统

> 关键词：Vue 后台、公共组件、render 函数复杂表格、TinyMCE、ECharts

### Q1：你在这里大量使用 render 函数构建复杂表格。为什么当时选择 render 函数，而不是纯模板？你是如何让这套表格写起来不那么痛苦的？

- **若能说到动态列、slot 渲染、配置驱动**：
    
    - 追问1：现在回头看，你会不会选择用 JSX 或者表格配置 JSON 来重构？原因是什么？
        
    - 追问2：这套经验对你后面做后台管理系统有什么复用？
        
- **若回答泛，仅说“方便动态表格”**：
    
    - 追问3：给你一个需求：列字段和显示方式完全由运营配置，你会如何设计“表格配置 + render 渲染”的结构？
        

---

## 九、个人全栈项目 Andy-Admin（含 Express 后端）

> 关键词：React18 + Redux Toolkit、TS 封装 Axios、路由懒加载 & 权限、前后端规范、JWT、Mongo + Redis、防 DOS

### Q1：你在这个项目里用 TS 对 Axios 做了二次封装，还做了取消重复请求、错误拦截等。整体封装思路是什么？哪些细节你觉得可以拿出来在团队里复用？

- **若能讲到：统一实例、拦截器、请求 key、AbortController、返回类型**：
    
    - 追问1：在类型层面，你有做过“接口响应类型自动推导”或者“错误类型约束”吗？是怎么实现的？
        
    - 追问2：如果把这套封装做成 npm 包，你会提供哪些可配置项，以适配不同项目的需求？
        
- **若回答比较泛**：
    
    - 追问3：假设现在后端要迁移到 GraphQL 或 gRPC，你觉得这套封装还能复用多少？需要改哪些核心点？
        

---

### Q2：整个项目从前端到后端都由你自己搭建，包括 JWT 鉴权、路由权限、反 DOS 等。你觉得如果让你把这套东西迁移成“公司内部的脚手架模板”，你会怎么改？

- **若能说出：抽出 auth 中间件、权限模型、CI 模板、lint 规范**：
    
    - 追问1：在路由和按钮权限控制上，你采用的是角色模型还是基于资源的权限模型？未来要支持更细粒度权限时怎么扩展？
        
    - 追问2：防 DOS 中间件在实际应用中会不会误伤正常用户？你如何设置限流阈值和白名单？
        
- **若回答泛**：
    
    - 追问3：如果你加入我们的项目组，需要把我们现有的前端项目“工程化规范提升一档”，你会先从哪三件事做起？
        

---

你可以根据你关心的方向（比如：**monorepo / 工程化 / TS 复杂类型 / AI 工作流 / 三端小程序 / RN**）优先挑对应项目来聊；如果你告诉我你面试时更想压时间在某几个项目上，我也可以帮你把那几块再进一步“缩成 5～6 个关键必问问题”。