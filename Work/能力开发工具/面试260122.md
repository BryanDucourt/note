# 1 岳珂
# 候选人概览（快速定位）

候选人技能覆盖面很广：Golang、Linux 生产环境、Shell、MySQL/Redis、MinIO/Nats、gin/go-kit/go-zero、gRPC、DDD、Docker/K8s、可观测性、CI/CD 从 0 到 1。  
项目侧重点更偏 **平台型后端 + 调度 + 交付体系**。

---

# 项目一：小米健康 APP（2025.04–2025.08）

> 技术栈：Golang、MySQL、Redis、RocketMQ、K8s  
> 负责：跑团 v2.0 API 设计实现 + e2e；消息中心；高并发高可用优化（压测、缓存、MQ 消费等）

## Q1：跑团 v2.0 的 API 你负责“功能设计 + 实现 + e2e”，你当时是怎么做接口设计和版本演进的？

- **如果回答具体**（能讲清 RESTful、幂等、分页、版本策略）：
    
    - 追问1：你们跑团核心接口有没有“重复提交/重复参团/重复支付”的问题？你如何保证幂等？
        
    - 追问2：如果一个接口要兼容老版本客户端，你们怎么做向后兼容？是 URL 版本、Header 版本还是字段兼容？
        
- **如果回答偏泛**：
    
    - 追问3：你挑一个核心接口（比如报名/退出/查询跑团详情），把 request/response、状态码、异常分支完整说一下。
        

## Q2：你提到“消息中心的设计和开发”，这个消息中心的模型是什么？是站内信、事件流还是通知系统？

- **如果回答具体**（提到消息类型、模板、推送、未读计数、拉取/推送）：
    
    - 追问1：未读数你们怎么做？Redis 计数 + DB 落库？如何防止计数不一致？
        
    - 追问2：消息投递失败如何补偿？有没有重试队列/死信队列？
        
- **如果回答偏泛**：
    
    - 追问3：如果消息量暴涨（例如活动期间），你觉得瓶颈最可能在 DB、Redis 还是 MQ？你会怎么压测验证？
        

## Q3：你做了高并发高可用优化，包括“数据库压测、缓存处理、MQ 消费”。你能举一个你主导的优化案例吗？

- **如果回答具体**（慢 SQL、索引、缓存击穿、消费者积压）：
    
    - 追问1：你们压测指标看哪些？QPS、P95、错误率、连接数、锁等待？
        
    - 追问2：RocketMQ 消费积压时，你们怎么扩容消费者？怎么避免重复消费？
        
- **如果回答偏泛**：
    
    - 追问3：如果线上突然出现 P99 飙升但平均耗时正常，你第一反应会从哪几个维度排查？
        

---

# 项目二：易恒模糊测试平台（2022.07–2025.03）

> 智能模糊测试平台（白盒/灰盒/黑盒/auto harness/AI 模式），分布式微服务，涉及 agent 调度、探活、语料解析、上下文融合算法；MinIO、Nats、Docker/K8s；并支持 compose/K8s 可切换部署。

## Q1（强岗位相关）：你在这个平台里做了“agent 调度架构设计与实现”，调度系统的核心模型是什么？

- **如果回答具体**（任务→队列→agent→执行→回传→状态机）：
    
    - 追问1：任务状态机怎么设计？如何处理 agent 执行到一半崩溃/断联？
        
    - 追问2：如何避免同一个任务被多个 agent 重复执行？你们用分布式锁还是租约机制？
        
- **如果回答偏泛**：
    
    - 追问3：你画一下关键流程：任务创建 → 分发 → 执行 → 上报结果 → 重试/失败终止，分别由哪些服务负责？
        

## Q2：你提到“代理层业务、探活、HTTP 参数解析为语料、接口上下文及数据融合算法”，探活是怎么做的？

- **如果回答具体**（心跳、健康检查、超时剔除、灰度权重）：
    
    - 追问1：探活频率如何设置？如果 agent 规模很大，探活会不会反过来压垮系统？
        
    - 追问2：探活判断“可用”是只看进程存活，还是包含 CPU/内存/磁盘/队列长度等指标？
        
- **如果回答偏泛**：
    
    - 追问3：如果出现“探活误判导致任务大量失败”，你会怎么定位根因？
        

## Q3：你们做了“对 Docker 和 K8s 二次开发，实现 compose / K8s 可切换部署”，这块你怎么设计抽象层的？

- **如果回答具体**（统一描述模型、适配器、运行时差异）：
    
    - 追问1：compose 和 K8s 的差异非常大（网络、服务发现、卷、资源限制），你们抽象时哪些能力做成通用，哪些只能分叉？
        
    - 追问2：切换部署方式时，如何保证配置一致性？有没有配置中心/模板化？
        
- **如果回答偏泛**：
    
    - 追问3：如果让你现在再做一次，你会坚持“统一抽象”还是直接拆两套实现？为什么？
        

---

# 项目三：金地物业管理系统（2021.07–2021.11）

> go-zero 微服务 + Vue 前端；地图/GIS、审批流转、报表大屏  
> 服务划分：签约管理 / 数据管理 / 统计分析 3 个服务；沉淀组件库和通用脚手架。

## Q1：你当时把系统拆成 3 个微服务，拆分依据是什么？按领域拆还是按团队拆？

- **如果回答具体**（DDD、边界上下文、数据隔离）：
    
    - 追问1：跨服务的事务怎么处理？比如签约成功后要影响统计数据，怎么保证一致性？
        
    - 追问2：服务之间通信用的什么方式（HTTP/gRPC/Kafka）？为什么这么选？
        
- **如果回答偏泛**：
    
    - 追问3：如果后面要新增“审批流服务”，你会怎么接入而不影响现有服务边界？
        

## Q2：你沉淀了模块化通用组件库和服务脚手架，具体沉淀了哪些能力？

- **如果回答具体**（日志、鉴权、配置、错误码、链路追踪）：
    
    - 追问1：脚手架如何保证“统一规范”同时又允许业务灵活？
        
    - 追问2：你们怎么控制脚手架版本升级？会不会出现项目 A 升了、项目 B 不敢升？
        
- **如果回答偏泛**：
    
    - 追问3：你给我举一个“你沉淀过但后来证明很有价值”的组件或中间件封装。
        

## Q3：go-zero 你熟悉的话，能讲讲它在微服务治理方面你最认可的点是什么？

- **如果回答具体**（限流、熔断、服务发现、链路追踪）：
    
    - 追问1：你在真实生产里踩过 go-zero 哪些坑？比如配置复杂、可观测性接入、框架限制？
        
- **如果回答偏泛**：
    
    - 追问2：如果让你换成 go-kit 或 gin 自建，你觉得工程成本主要增加在哪？
        

---

# 加分综合题（任务调度岗强相关，拉开层级）

## Q：你做过 agent 调度平台，如果我们要做一个“通用任务调度系统”，要求：

- 支持定时任务 + 依赖 DAG
    
- 支持任务重试 + 超时控制
    
- 支持 worker 动态扩缩容
    
- 支持多租户隔离
    

你会怎么拆模块？核心数据模型怎么设计？

**追问（看架构能力）**

- 任务状态机怎么定义？哪些状态必须落库？
    
- 如何保证“同一任务只被执行一次”？（租约 / 分布式锁 / 乐观锁 / 去重表）
    
- 如何做可观测性？你会埋哪些关键指标？
    
- 调度器自身如何 HA？如何避免单点？
    

---

# 面试官建议：怎么快速判断他是否“真做过调度平台”

你重点听他能不能主动说出这些关键词（不说也可以追问逼出来）：

- **幂等 / 去重 / 重试策略（指数退避）**
    
- **任务状态机 + 补偿机制**
    
- **worker 探活 + 租约**
    
- **MQ 堆积处理**
    
- **压测指标（P95/P99）**
    
- **链路追踪落地（traceID 贯穿）**
    
- **K8s 下的扩缩容与资源限制**
    

能把这些讲清楚，基本就是“做过且踩过坑”的级别。
# 2 张万龙

# 项目一：自动驾驶数据闭环工具链平台（2023.11–至今）

> 关键词：仿 Kubeflow Notebook、训练管理、PyTorch/TensorFlow/YOLOv7、MinIO + BeeGFS 数据湖

## Q1：你提到“仿照 Kubeflow Notebook 在线编辑器功能，搭建相同 Notebook 功能”，你们这个 Notebook 的核心能力有哪些？整体架构怎么做的？

- **如果回答具体**（提到多租户、资源隔离、镜像、PVC、权限）：
    
    - 追问1：Notebook 的“隔离”怎么做？是 K8s Namespace 隔离，还是容器级别资源限制（CPU/Memory/GPU）？
        
    - 追问2：Notebook 的持久化存储怎么处理？用户的代码、数据、模型分别落在哪？
        
- **如果回答偏泛**（“就是起一个容器跑 Jupyter”）：
    
    - 追问3：如果两个人同时启动大模型训练 Notebook，把集群打满了，你们怎么做配额/限流/优先级？
        

## Q2：你集成了 PyTorch / TensorFlow / YOLOv7 做训练管理，这套“训练任务调度”是怎么设计的？

- **如果回答具体**（任务队列、Job CRD、状态机、重试）：
    
    - 追问1：训练任务失败你们怎么处理？是自动重试、断点续训还是人工介入？状态怎么记录？
        
    - 追问2：如果要支持“分布式训练”（多卡/多机），你们的调度层要做哪些改造？
        
- **如果回答偏泛**：
    
    - 追问3：你说“模型训练管理功能”，那你举一个训练任务从创建到完成的完整链路：参数怎么传、日志怎么看、结果怎么回传？
        

## Q3：你们搭建了 MinIO + BeeGFS 的数据湖/存储，为什么两种存储都要用？各自解决什么问题？

- **如果回答具体**（对象存储 vs 并行文件系统、吞吐、成本）：
    
    - 追问1：训练读写数据的热点在 BeeGFS 还是 MinIO？怎么避免训练时 IO 打满导致任务变慢？
        
    - 追问2：数据生命周期怎么做？原始数据、清洗数据、训练产物如何分层管理？
        
- **如果回答偏泛**：
    
    - 追问3：如果让你只保留一种存储（只用 MinIO 或只用 BeeGFS），你会怎么选？代价是什么？
        

---

# 项目二：云原生平台（KubeSphere 二次开发）（2022.10–2023.10）

> 关键词：多租户云原生 OS、多集群管理、DevOps、日志/监控告警、KubeVirt、KubeOVN

## Q1：你提到“KubeSphere 二次开发，维护 DevOps、监控告警、存储管理、日志查询与收集”，你具体负责哪一块？改动点在哪里？

- **如果回答具体**（改 CRD、controller、API、前后端交互）：
    
    - 追问1：监控告警是基于 Prometheus 的话，你们告警规则是谁维护？如何做多租户隔离？
        
    - 追问2：日志查询收集用 ES 的话，如何控制索引膨胀？怎么做冷热分层？
        
- **如果回答偏泛**：
    
    - 追问3：你举一个“你改过的功能点”，从需求到代码落地，说清楚你改了哪些模块、如何验证上线。
        

## Q2：你用 KubeVirt + KubeOVN 构建虚拟机创建平台。VM 生命周期管理怎么做？网络是怎么分配的？

- **如果回答具体**（VM CR、IPAM、网段隔离）：
    
    - 追问1：VM 创建失败或卡住时，你们怎么排查？主要看哪些资源（PVC、网络、镜像拉取）？
        
    - 追问2：多租户场景下，VM 网络隔离如何保证？是否做了安全组/NetworkPolicy？
        
- **如果回答偏泛**：
    
    - 追问3：如果现在要支持“VM 热迁移”，你觉得最大难点是什么？
        

---

# 项目三：云汉芯城电商平台（2021.03–2023.10）

> 关键词：B 端电商、ERP、促销系统、优惠券/抽奖/短信、微服务重构、高并发

## Q1：你独立开发了优惠券、抽奖、专题、短信发送、划线价、促销系统。你挑一个“最复杂的模块”讲讲数据模型和核心流程。

- **如果回答具体**（优惠券领取/核销/冻结、抽奖概率、风控）：
    
    - 追问1：如何防止用户刷券/刷抽奖？有没有做限流、风控、幂等？
        
    - 追问2：促销叠加规则（满减+券+会员价）如何避免逻辑爆炸？你怎么设计扩展点？
        
- **如果回答偏泛**：
    
    - 追问3：如果让你现在加一个“阶梯满减 + 指定品类互斥”的活动规则，你会怎么在现有系统上扩展？
        

## Q2：你提到“项目可支撑百亿目标下的高并发请求”，具体你们的高并发场景是什么？QPS 量级是多少？怎么扛住的？

- **如果回答具体**（缓存、削峰、MQ、分库分表、热点 key）：
    
    - 追问1：库存扣减、下单支付这种强一致业务，你们如何保证不超卖？
        
    - 追问2：缓存击穿/雪崩怎么处理？热点商品如何治理？
        
- **如果回答偏泛**：
    
    - 追问3：你讲一个“你亲自处理过的线上性能问题”，怎么定位、怎么优化、怎么验证。
        

## Q3：你用 Golang 对公司业务做微服务重构，拆分依据是什么？拆完后怎么治理？

- **如果回答具体**（按领域拆、服务间通信、链路追踪）：
    
    - 追问1：跨服务事务怎么处理？比如订单创建成功但促销核销失败，怎么补偿？
        
    - 追问2：服务治理你们用什么做？（注册发现、限流熔断、配置中心、日志/指标/追踪）
        
- **如果回答偏泛**：
    
    - 追问3：你觉得微服务改造“最容易踩坑”的点是什么？你踩过吗？怎么解决的？
        

---

# 项目四：handata 系统（报表分析 / 上亿级数据）（2018.04–2021.03）

> 关键词：用户行为统计、埋点、TiDB、阿里日志服务、Redis、ECharts、Beego

## Q1：handata 做“用户行为轨迹统计报表”，埋点数据怎么采集、怎么清洗、怎么入库？

- **如果回答具体**（日志服务 → 消费 → 清洗 → TiDB/ClickHouse）：
    
    - 追问1：埋点事件怎么设计 schema？如何兼容版本演进（字段新增/修改）？
        
    - 追问2：报表查询很慢时，你怎么优化？预聚合、宽表、物化视图你用过吗？
        
- **如果回答偏泛**：
    
    - 追问3：你说“数据量上亿级”，那你讲一个典型报表从查询到展示，链路里最耗时的点在哪里？
        

## Q2：你们用 TiDB 扛报表数据，为什么不用 MySQL？TiDB 的坑你踩过哪些？

- **如果回答具体**（事务、索引、热点、region）：
    
    - 追问1：TiDB 在大范围扫描、排序、join 的场景性能如何？你们如何规避？
        
    - 追问2：如果某张表变成热点表，你们怎么拆或怎么降热点？
        
- **如果回答偏泛**：
    
    - 追问3：如果今天让你重做 handata，你会坚持用 TiDB 还是换 ClickHouse？为什么？
        

---

# 项目五：图盾系统（图片对比维权 / 百亿级数据）（2018.04–2021.03）

> 关键词：采集/对比、ES、RabbitMQ、gRPC、Colly、Python/Django、权限/任务管理自动化

## Q1：图盾系统要在百亿级数据量中做图片对比，你们是怎么“降维/加速”图片相似度检索的？

- **如果回答具体**（特征向量、hash、倒排索引、ES 存特征）：
    
    - 追问1：ES 适合做向量检索吗？你们当时用的是 hash 还是别的策略？
        
    - 追问2：对比任务是批处理还是实时？如何用 MQ 做削峰？
        
- **如果回答偏泛**：
    
    - 追问3：如果现在要把对比速度提升 10 倍，你会从算法侧还是系统侧先下手？为什么？
        

## Q2：采集任务管理与自动化这块，你们如何防止重复采集、如何做失败重试？

- **如果回答具体**（任务状态机、幂等、分布式锁）：
    
    - 追问1：采集任务并发很高时，如何控制目标站点的压力，避免被封？
        
    - 追问2：采集结果的“质量”怎么评估？有没有采集成功率/覆盖率指标？
        
- **如果回答偏泛**：
    
    - 追问3：你讲一个采集链路里最难排查的问题是什么？你怎么解决的？
        

---

# 项目六：图片下载系统（安全管理 + 鉴权 + 反盗链）（2018.04–2021.03）

> 关键词：统一下载、鉴权、异常通知、攻击拦截、统计报表

## Q1：你说“上线后拦截大量下载攻击”，你们识别攻击的策略是什么？如何做到不误伤正常用户？

- **如果回答具体**（限流、token、签名、UA/IP 规则）：
    
    - 追问1：下载链接是一次性签名 URL 还是长期 URL？签名如何防止被转发盗用？
        
    - 追问2：如果攻击方模拟正常用户行为（低频慢刷），你们怎么检测？
        
- **如果回答偏泛**：
    
    - 追问3：如果现在要加一个“按用户/按业务线的下载配额”，你会怎么设计？
        

## Q2：下载统计报表你们怎么做？实时还是离线？对业务有什么价值？

- **如果回答具体**（埋点、聚合、报表）：
    
    - 追问1：报表的核心指标有哪些？PV/UV、成功率、失败原因、异常分布？
        
    - 追问2：如何保证统计数据可信？有没有对账机制？
        
- **如果回答偏泛**：
    
    - 追问3：如果老板要一个“实时大屏”，你觉得系统需要补哪些能力？
        

---

# 任务调度岗“拔高题”（强建议最后问）

这位候选人有很多“任务型系统”经验（采集、训练、下载、防刷），非常适合用这题拉开层级：

## Q：如果让你设计一个“通用任务调度系统”，支持：

- 定时任务 + DAG 依赖
    
- worker 探活 + 自动扩缩容
    
- 失败重试 + 超时控制
    
- 幂等 + 去重
    
- 多租户隔离
    

你会怎么拆模块？核心表结构怎么设计？

**追问（看是否真做过调度）**

- 调度器如何 HA？如何避免单点？
    
- 如何保证同一任务不被重复执行？（租约 / 分布式锁 / 乐观锁）
    
- MQ 堆积如何处理？如何快速恢复？
    
- 可观测性怎么做？你会监控哪些指标（P95/P99、积压、失败率、重试次数）？
    

---

## 面试官建议（这位怎么判断水平）

这份简历“覆盖面很大”，你需要重点确认两点：

1. **是否真的做过“高并发/大数据”的核心链路**  
    👉 追问他“QPS/数据量/耗时/P95/P99/压测结果”，能说出数字更可信。
    
2. **是否能讲清“系统如何避免出错”**  
    👉 重点听：幂等、补偿、重试、去重、状态机、限流。
# 3 黄懿伟
# 候选人亮点快速定位

- **核心业务负责人**：互动视频签后端技术负责人，日均 **2 万+** 签约单量，复杂网络环境下可用性 **99.99%**
    
- **流媒体链路重构经验**：SRS → 腾讯云直播；录制存储链路去 NFS 中转，视频处理 **30s → 5s**
    
- **存证系统重构**：C# → GoFrame + Kafka consumer + PDF 出证链路（wkhtmltopdf/pdfcpu）
    

> 这个候选人非常适合用来考察：**任务异步化、状态机/幂等、MQ 消费扩缩容、链路性能优化、生产事故预防**。

---

# 项目一：互动视频签署系统（后端技术负责人）

> 直播推流 + AI 人脸识别 + 小程序；WebSocket/RTMP；CDN；SRS → 腾讯云直播；录制直存 COS；海外加速；出证体验优化

## Q1：你们“互动视频签”整体架构怎么设计的？从用户进入小程序到完成签署，服务端链路能讲一下吗？

- **如果回答具体**（能讲清：鉴权→推流→AI→签署→存证→出证）：
    
    - 追问1：在你们这个链路里，**哪些步骤必须强一致**（不能丢），哪些可以最终一致？
        
    - 追问2：你们是怎么做“证据链完整性”的？比如签署动作、视频、AI 识别结果如何绑定同一个签约单？
        
- **如果回答偏泛**（“就是推流+识别+签署”）：
    
    - 追问3：你挑一个关键动作（比如“开始签署/确认签署”），说清楚**后端落库字段、状态变化、失败重试**怎么做。
        

---

## Q2：你提到采用 CDN 解决推流卡顿，并把流媒体从 SRS 迁移到腾讯云直播来解决海外卡顿。迁移的关键决策点是什么？

- **如果回答具体**（海外节点、QoS、延迟、带宽、运维成本）：
    
    - 追问1：迁移过程中最大的坑是什么？比如回调差异、录制、鉴权、延迟抖动，你怎么解决的？
        
    - 追问2：如果腾讯云直播回调丢了/延迟了，你们怎么保证业务不丢状态？有没有补偿机制？
        
- **如果回答偏泛**：
    
    - 追问3：你如何证明“迁移确实有效”？有没有量化指标（卡顿率、首帧时间、失败率、P95 延迟）？
        

---

## Q3：你把录制存储链路改成“直存 COS”，去掉 NFS 中转，处理时间从 30 秒降到 5 秒。你具体改了哪些环节？

- **如果回答具体**（录制回调、转码/封装、异步队列、并发控制）：
    
    - 追问1：30 秒里原本最耗时的是哪一步？IO、转码、上传、还是串行流程？你怎么定位的？
        
    - 追问2：如果 COS 上传失败或超时，你们怎么处理？会不会导致“签完了但视频证据丢了”？
        
- **如果回答偏泛**：
    
    - 追问3：你把这个优化当成一次“性能项目”来看，**你做了哪些 profiling 或压测**？最终如何验证 83% 提升？
        

---

# 项目二：签署文件存证证明/出证服务（C# → Go 重构主导）

> GoFrame、Kafka consumer、HTML 模板填充 → wkhtmltopdf → pdfcpu 背景处理 → 盖章；对外鉴权；异步同步公证处

## Q1：你主导把 C# 服务重构成 Go 服务，为什么要重构？重构后性能/稳定性提升体现在哪里？

- **如果回答具体**（吞吐、延迟、部署、维护成本、并发模型）：
    
    - 追问1：你们重构是“全量切换”还是“双写/灰度”？如何保证迁移过程中业务不中断？
        
    - 追问2：重构时最难的是哪块？接口兼容、历史数据、PDF 生成一致性、还是异步链路？
        
- **如果回答偏泛**：
    
    - 追问3：你举一个最典型的接口，说清楚：旧系统怎么做、新系统怎么做、你改了哪些关键设计。
        

---

## Q2：你新增了 Kafka consumer 处理异步任务。你们 Kafka 里消息是怎么设计的？如何保证幂等和可重试？

- **如果回答具体**（消息 key、去重表、幂等 token、重试队列、DLQ）：
    
    - 追问1：consumer 扩容时如何避免同一任务重复执行？你们依赖 Kafka 分区语义还是业务幂等？
        
    - 追问2：如果出现“消息消费成功但 DB 写失败”，你怎么保证最终一致？
        
- **如果回答偏泛**：
    
    - 追问3：你描述一下你们的“失败处理策略”：重试几次、间隔多久、什么时候进入死信、怎么人工补偿？
        

---

## Q3：出证链路里你用了 wkhtmltopdf + pdfcpu，并解决了“背景被切割”的问题。你是怎么定位并解决的？

- **如果回答具体**（PDF 页面尺寸、CSS、渲染引擎差异、裁剪规则）：
    
    - 追问1：你们如何保证“同一份存证内容”多次生成 PDF 的结果一致（像素级一致/版式一致）？
        
    - 追问2：如果客户上传的内容非常复杂（长表格、超长图片），你们怎么避免 PDF 生成超时或 OOM？
        
- **如果回答偏泛**：
    
    - 追问3：如果让你重做一次出证系统，你还会坚持 HTML→PDF 这条路线吗？有没有替代方案？
        

---

# 项目三：法大大电子签署证据中心（存证中心后端）

> 收集实名认证、CA 授权、印章、签署过程数字信息；Kafka 异步削峰；MongoDB 存储；缓存+索引；任务队列异步出证与回调

## Q1：证据中心为什么用 MongoDB 而不是 MySQL？你们的存证数据模型是怎么设计的？

- **如果回答具体**（文档结构、扩展性、查询模式、索引设计）：
    
    - 追问1：你们最核心的查询场景是什么？按合同ID查？按用户查？按时间查？索引怎么建？
        
    - 追问2：Mongo 数据量上来以后如何做归档/分片？有没有遇到性能瓶颈？
        
- **如果回答偏泛**：
    
    - 追问3：你给一个存证 document 的结构示例（字段层级），以及对应的查询方式。
        

---

## Q2：你们用 Kafka 异步处理来保证高并发和快速响应。哪些任务必须异步？哪些必须同步？

- **如果回答具体**（同步写核心存证、异步同步公证处/生成报告/回调）：
    
    - 追问1：如果用户提交存证后立刻要“可查询”，异步链路还没处理完怎么办？前端展示什么状态？
        
    - 追问2：异步任务积压时，如何保证 SLA？是扩 consumer、还是降级？
        
- **如果回答偏泛**：
    
    - 追问3：如果 Kafka 整体不可用 10 分钟，你的系统会发生什么？用户体验怎么兜底？
        

---

## Q3：你提到“任务队列异步出证与回调通知，保证服务解耦”。回调通知如何设计才能避免重复回调/漏回调？

- **如果回答具体**（回调状态表、重试、签名校验、幂等）：
    
    - 追问1：客户回调接口超时/500，你们怎么重试？最大重试次数和退避策略是什么？
        
    - 追问2：客户重复收到回调怎么办？你们如何让客户侧幂等？
        
- **如果回答偏泛**：
    
    - 追问3：你举一个“回调失败导致线上事故”的例子（如果有），你怎么修复并防止复发？
        

---

# 强相关拔高题（任务调度岗位必问）

> 这位候选人“异步任务 + MQ + consumer 扩容 + 状态管理”很契合任务调度岗位，建议用这题拉开层级。

## Q：如果让你设计一个“通用任务调度系统”，要求：

- 支持任务状态机（pending/running/success/fail/retry）
    
- 支持重试（指数退避）
    
- 支持 worker 横向扩容
    
- 支持任务幂等（避免重复执行）
    
- 支持回调通知（至少一次投递）
    

你会怎么设计核心表结构和调度流程？

**追问（看是否真做过）**

- 如何避免“任务被执行两次”？（租约/乐观锁/去重表）
    
- 如何处理“执行成功但回调失败”？
    
- 如何做可观测性（任务积压、失败率、重试次数、P95 执行耗时）？
    
- 如果调度器宕机，如何恢复？如何保证不丢任务？
    

---

# 面试官建议：这位怎么快速判断“真高级 or 只会写业务”

你重点听他能否主动提到这些关键点（提不到就追问逼出来）：

- **状态机 + 幂等**
    
- **Kafka 消费扩容与重复消费处理**
    
- **失败补偿机制（重试/死信/人工兜底）**
    
- **线上指标（P95/P99、积压、超时）**
    
- **迁移/重构策略（灰度/回滚）**
    

如果他能讲清楚并且有数据/事故复盘经验，这个人会非常适合“任务调度系统开发工程师”。

---

如果你还要我继续整理下一位候选人，我可以继续按同样格式输出。