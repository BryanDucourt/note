# 总体评价
1 刘东明 技术深度有限，沟通比较顺畅，可以考虑
2 周世林 给人的感觉有点油，技术泛泛而谈 pass
3 符伟豪 技术深度比刘东明差一点，沟通还可以，暂定
4 王国强 啥啊这是 pass
# 1 刘东明
## 一、健宁医药后台管理（React + Redux + Antd）

> React + react-router-dom + Redux/Redux-thunk + Antd，ECharts，可复用搜索/筛选组件等。

### Q1：你在健宁医药后台里负责代理人管理、订单和库存等模块，这几个模块在前端的**状态管理和数据流设计**是怎样的？

- **如果回答比较具体**（能说出 Redux 模块划分、异步 action、数据归一化等）：
    
    - 追问1：在一个典型的「下单 → 出库 → 库存变更」链路里，Redux 里的 state 是怎么变化的？你会怎样避免不同模块之间 state 冗余或不一致？
        
    - 追问2：你们当时用 Redux + Thunk，如果让你现在重做，会考虑用 Redux Toolkit / RTK Query 或其他方案吗？为什么？
        
- **如果回答比较泛**（只说“用 Redux 管状态、调接口”）：
    
    - 追问3：那你挑一个你最熟的页面（比如订单列表），从用户点击页面开始，到页面数据渲染出来，中间经过了哪些关键代码路径？你顺着讲一遍。
        

---

### Q2：你提到封装了搜索框、筛选组件、防抖/节流函数来做可复用组件。这个「列表 + 搜索/筛选」的组件体系是怎么设计的？

- **如果回答比较具体**（能说清 props 设计、回调、组合方式）：
    
    - 追问1：不同业务模块的搜索条件会很不一样，你在封装时如何平衡「通用性」和「不至于变成巨大杂糅组件」？
        
    - 追问2：防抖/节流在这里具体放在哪一层？你们如何保证不会因为防抖导致用户感觉“点了没反应”？
        
- **如果回答比较泛**：
    
    - 追问3：假设产品现在提一个新需求：在现有列表上增加「高级筛选（支持多条件组合、保存筛选方案）」功能，你会基于现有封装怎么扩展？
        

---

### Q3：后台里有数据可视化，用 ECharts 做图表。你负责的图表主要展示什么？你是怎么组织图表与业务数据的关系的？

- **如果回答具体**（能说到图表类型、数据结构、刷新策略）：
    
    - 追问1：当筛选条件变化时，图表刷新策略和列表刷新策略是一致的吗？有没有做过“局部刷新”优化？
        
    - 追问2：有没有遇到过图表在极端数据量下卡顿的问题？你是如何定位并优化的？
        
- **如果回答比较泛**：
    
    - 追问3：如果现在要把一个「表格 + 图表联动」的页面拆成可复用组件，你会在什么层级做拆分？各自负责什么？
        

---

## 二、益药康（移动端 React 电商平台）

> React + Hooks，路由 & 图片懒加载，react-router-cache-route 缓存，百度地图轨迹/标记等。

### Q1：在移动端电商场景里，你做了路由懒加载、组件缓存、shouldComponentUpdate 等优化。你觉得**最明显提升体验的一个点**是哪一个？具体是怎么做的？

- **如果回答具体**（能说到缓存策略、路由切换体验、渲染次数对比等）：
    
    - 追问1：在使用 react-router-cache-route 时，你们是怎么决定“哪些页面需要缓存、哪些不需要”的？有踩过什么坑吗？
        
    - 追问2：你在什么场景下会选择用 shouldComponentUpdate，而在什么场景下更倾向于用 memo / useMemo / useCallback？
        
- **如果回答泛**：
    
    - 追问3：假设现在发现商品列表滑动时偶发卡顿，你会从哪些方面开始排查？
        

---

### Q2：简历里提到你用百度地图做轨迹和地点标记。这个功能在业务里的具体场景是什么？前端是怎么串起来的？

- **如果回答具体**（讲到轨迹点数据结构、地图 SDK 的调用、Marker 管理）：
    
    - 追问1：轨迹点通常很多，你是如何控制渲染数量和更新频率的，避免地图上“点太多卡成 PPT”？
        
    - 追问2：如果用户需要在地图上筛选特定时间段或订单的轨迹，你会如何在前端做数据裁剪和交互设计？
        
- **如果回答泛**：
    
    - 追问3：如果让我现在把这个轨迹功能移植到另一个项目，你会怎么封装成可复用的地图组件？
        

---

## 三、Venus 实时数据采集平台（爱奇艺，Vue2 → Vue3 + Vite + AI）

> Vue + Vuex/Pinia，Webpack → Vite，Element Plus，ECharts / 飞书 VChart，自适应大屏，fetchEventSource 流式输出，接入大模型做平台助手等。

### Q1：Venus 项目从 Vue2 + Webpack 升级到 Vue3 + Vite、Vuex 升级到 Pinia，是怎么规划和落地的？你本人参与了哪些关键步骤？

- **如果回答具体**（能说出迁移策略、兼容层、逐步替换）：
    
    - 追问1：在迁移过程中，最棘手的兼容问题是什么？比如第三方库、全局混入、this 指向等，你是怎么解决的？
        
    - 追问2：你们有没有为迁移专门设计“双运行环境”或开关，让新旧版本并行一段时间？如果有，是怎么做的？
        
- **如果回答泛**：
    
    - 追问3：假设现在我给你一个大型 Vue2 项目，让你在不长停机时间的前提下迁到 Vue3 + Vite，你会给出怎样的分阶段计划？
        

---

### Q2：你写到使用 fetcheventsource 做流式输出、结合 VChart、Markdown、Element Plus 完成 AI 对话和平台助手。这套**前端流式渲染链路**是怎么设计的？

- **如果回答具体**（能说明事件流处理、Buffer、增量渲染等）：
    
    - 追问1：在流式输出时，你如何避免频繁 setState / DOM 更新带来的卡顿？有没有做批量更新或节流？
        
    - 追问2：流式结果中如果包含结构化信息（比如图表数据、Markdown 片段），你们如何从纯文本中解析出并驱动 VChart 或 Markdown 渲染？
        
- **如果回答泛（“就一边收一边 append”）**：
    
    - 追问3：当 SSE 连接中断或超时时，你会如何做重连和错误提示？怎样避免重复生成同一段内容？
        

---

### Q3：平台是大数据可视化场景，你们用了 ECharts 和飞书 VChart，并做等比例缩放适配多端屏幕。你具体是怎么处理「大屏自适应」的？

- **如果回答具体**（有设计“基准分辨率 + scale 缩放”、监听 resize 等）：
    
    - 追问1：等比例缩放在某些极端分辨率下会造成文字过小或过大，你们有做过补救措施（比如字体最小值、换行规则）吗？
        
    - 追问2：ECharts / VChart 在缩放后需要调用什么 API 或做什么处理，才能保证图表不模糊不错位？
        
- **如果回答泛**：
    
    - 追问3：如果现在要在这个项目里加一个“多屏联动展示”（一个页面拆成多个物理屏展示）的需求，你觉得在现有自适应方案上会遇到哪些问题？
        

---

## 四、DSMP 数据安全管理平台（React + Antd Pro + TS）

> React Hooks + antd-pro + AntV 图表，useModel 管理状态，Axios 二次封装，SSO 接入、权限路由、PC+移动端兼容等。

### Q1：你在 DSMP 里封装了 Axios、做了 SSO 对接和权限路由。整体的**认证与权限体系**从前端视角是怎么设计的？

- **如果回答具体**（能讲到拦截器、token/票据、401 处理、路由守卫）：
    
    - 追问1：当 SSO 返回的权限发生变化时，你们如何保证前端路由和菜单即时更新？是重新拉权限还是依赖缓存？
        
    - 追问2：你们有没有区分“接口级权限错误”和“前端路由未授权”的情况？在 UI 上如何向用户解释？
        
- **如果回答泛**：
    
    - 追问3：如果现在某些用户反馈“偶尔会被莫名其妙踢出登录”，你会怎样排查是 token 过期策略、时间同步问题还是前端拦截逻辑有 bug？
        

---

### Q2：DSMP 需要做分类分级、访问控制等数据安全可视化。你用 AntV 做了图表和大屏，这块你是怎么让**图表组件既通用又贴合业务**的？

- **如果回答具体**（能说出图表配置抽象、数据适配层、通用组件）：
    
    - 追问1：举一个你封装的图表组件例子，说明它的 props 设计和数据格式约定是怎样的？
        
    - 追问2：当业务要换一个维度/指标展示时，前端改动一般控制在什么范围内？有没有做到基本只改配置？
        
- **如果回答泛**：
    
    - 追问3：如果我们要在现有平台上快速加一个“敏感字段访问热力图”，你会如何利用你封装好的图表体系来落地？
        

---

### Q3：这个项目中你用了 TypeScript 来约束接口和状态。你觉得 TS 在这个项目里真正帮到你的地方有哪些？有没有踩过什么坑？

- **如果回答具体**（接口类型、useModel 返回值、组件 props 等）：
    
    - 追问1：在 Axios 封装里，你是怎么处理请求/响应的类型定义的？有没有用到泛型来推导数据类型？
        
    - 追问2：有没有遇到过“后端返回结构改变，TS 提前帮你发现问题”的例子？
        
- **如果回答泛**：
    
    - 追问3：如果我们现在有一套完全用 JS 写的大屏项目，让你用最小成本引入 TS，你会从哪几个模块先改起？
        

---

## 五、Stream 流数据服务平台（Vue + Element-UI / Vue3 + AntV）

> Vue2 + Element-UI，后续升级 Vue3，Axios 封装，ESLint 规范，数据分页表单、跨域 Proxy，后期用 AntV 搭建数据血缘和动态流程图。

### Q1：在 Stream 项目中，你做了 Axios 二次封装、统一管理 API、处理跨域和 token。你这套封装的**核心设计点**是什么？

- **如果回答具体**（拦截器、错误码统一处理、重试策略、取消请求等）：
    
    - 追问1：不同业务模块可能对错误处理有不同诉求，你是怎么在“统一处理”和“允许局部自定义”之间做平衡的？
        
    - 追问2：在跨域和 Proxy 这一块，你们在开发环境和生产环境的配置是否一致？如何避免上线后才暴露跨域问题？
        
- **如果回答泛**：
    
    - 追问3：如果我们现在要在这套封装上加“请求级别的超时和降级提示”，你会往哪里加，如何不影响原有调用方式？
        

---

### Q2：你提到后期在 Stream 项目中用 AntV 搭建数据血缘和动态流程图，这部分你具体参与了哪些？前端的数据结构和交互是怎么设计的？

- **如果回答具体**（节点/边模型、布局算法、交互事件）：
    
    - 追问1：数据血缘图通常节点很多，你们如何控制初始展示和层级展开，避免用户一上来就被“图淹没”？
        
    - 追问2：当用户在图上进行编辑（比如新增一条数据流）、或者筛选某条链路时，你们的状态管理和后端同步是怎样的？
        
- **如果回答泛**：
    
    - 追问3：假设我们现在想在这个血缘图上增加“实时告警染色”（出问题的链路变红），你会如何把实时数据和现有图结构结合起来？
        


- **Q7（技术广度）：** 你的简历中同时体现了 Vue 和 React 的深度使用。对比这两个框架，你觉得在**组件复用**和**状态管理**（如 Pinia vs Redux/useModel）方面，它们最大的区别是什么？你个人更倾向于哪一种，为什么？
    
    - 考察点： 技术选型思考能力，而非单纯的 API 熟练度。
        
- **Q8（AI 工具使用）：** 你提到熟练使用 Trae、Gemini 等 AI 工具。在实际开发中，除了让 AI 写简单的代码片段，你有没有利用 AI 解决过复杂的 Bug 或者进行架构设计的辅助？请举一个具体的例子。
    
    - 考察点： 验证是否真正利用 AI 提效，还是仅作为简历关键词。
        
- **Q9（学历背景）：** （注：候选人学历为2024年拿到的网教本科，之前可能是专科或非计算机专业，需委婉确认基础） 看到你是在职期间完成了计算机本科的学习。在系统学习计算机基础（如数据结构、网络协议）后，你觉得对你之前的编码习惯最大的改变是什么？
    
    - 考察点： 学习能力、理论与实践的结合。
# 2 周世林
## 一、智能化管理系统（Vue3 + TS + Pinia 等）

> 物流订单状态管理，20+ 版本迭代，对接 100+ 业务前端，自定义订单甘特图、组件库二次封装、Axios 二次封装、i18n、Pinia 一键翻转页面结构、权限控制、安全加密等。

### Q1：你从 0 搭这个智能化管理系统并迭代了很多版本，还要对接上百个业务前端。整体前端架构是怎样设计出来的？中间做过哪些关键重构？

- **如果回答比较具体**（能讲出模块划分、状态管理、组件抽象等）：
    
    - 追问1：在业务线这么多的情况下，你们是怎么做「通用模块/组件」和「各业务差异化需求」的边界划分的？举一个你们抽离出来的通用模块（比如文件导入导出、订单甘特图）详细说说。
        
    - 追问2：有哪一次版本重构是你印象最深的？当时是因为什么问题触发的（比如性能、可维护性、需求扩散），你是如何说服团队和推进落地的？
        
- **如果回答比较泛**（“就按业务拆页面，用 Vue3+Pinia 管理状态”）：
    
    - 追问3：那你挑一个你亲手做的子系统（比如订单甘特图那块），从「需求进入」到「上线」，前端具体经历了哪些设计和实现阶段？
        

---

### Q2：你提到对 Axios 做了二次封装，针对不同环境做跨域端口处理，并在拦截器里做全局效果处理。能具体讲讲这套网络层封装是怎么设计的吗？

- **如果回答比较具体**（能说出实例、多环境 baseURL、拦截器、错误处理）：
    
    - 追问1：不同环境（开发、测试、生产）以及「多个后端服务」的场景下，你们是怎么做配置管理的？是通过环境变量、配置中心还是运行时注入？
        
    - 追问2：全局效果处理具体包括哪些？比如 loading、全局报错、登录超时、重复请求取消，这些你们分别是怎么做的？有没有留出“局部覆盖”的接口？
        
- **如果回答比较泛**：
    
    - 追问3：假设现在要在现有封装上新增「请求级别的重试策略」和「统一的接口耗时埋点」，你会选择在哪一层做改动，如何保证对现有业务入侵最小？
        

---

### Q3：这个项目里你做了 i18n、Pinia 实现的“整体一键翻转页面结构”、权限控制、安全加密等。你觉得最有含金量的一块是哪一块？展开说说具体怎么做的。

- **如果他选权限 & 路由控制**：
    
    - 追问1：你们的权限模型是基于角色、资源，还是更细粒度的按钮级权限？在前端如何和路由、菜单、按钮展示打通？
        
    - 追问2：针对不同用户做路由控制，全局封装权限方法，这套东西在业务变化时扩展成本大吗？如果加一个新模块，一般要改哪些地方？
        
- **如果他选加密 & 安全**（对称加密用户信息）：
    
    - 追问1：对称加密是在哪一层做的？只在前端本地存储前加密，还是传输前也做处理？密钥管理是怎样设计的？
        
    - 追问2：你们有没有做过安全审计或攻防演练？在这个过程中发现过前端层面对安全意识不够的地方吗？
        
- **如果回答泛**：
    
    - 追问3：说说那个「一键翻转页面结构」是个什么需求？你是如何利用 Pinia 或其他手段做到“全局结构切换”的？
        

---

## 二、智慧物流平台（Web 端下单网站）

> 不同运输线路下单；RSA 加密用户信息，图片懒加载、视频压缩、订单详情页面封装、二维码跳转移动应用中心、无感刷新、防抖搜索、Node 版本管理等。

### Q1：智慧物流平台主要是下单场景，你们对用户信息做了 RSA 加密。前端这块你具体负责了什么？整个加密链路是怎么设计的？

- **如果回答比较具体**（讲到公钥加密、后端私钥解密、加密字段范围）：
    
    - 追问1：哪些数据是加密传输，哪些只在存储层做保护？你们如何平衡“安全性”和“接口调试、日志排查”的便利性？
        
    - 追问2：前端使用 RSA 加密通常会带来一些性能开销，你们有没有遇到体验上的影响？如果有，是怎么优化的（比如只在关键节点加密）？
        
- **如果回答泛**：
    
    - 追问3：假设现在要新增一个表单字段需要被加密，你会从哪里改起？你如何保证不会漏改或改错？
        

---

### Q2：你做了图片懒加载、视频压缩、无感刷新等优化。你觉得最显著改善体验的是哪一项？详细讲讲实现方式。

- **如果选懒加载/视频优化**：
    
    - 追问1：你们对图片懒加载的触发点（比如视口、滚动距离）是怎么设计的？如何避免反复触发加载？
        
    - 追问2：视频压缩是在前端处理、上传前降低码率，还是通过服务端转码？前端在这个过程中做了哪些配合？
        
- **如果选无感刷新（登录态 / token 刷新）**：
    
    - 追问1：你们是如何做到“用户基本无感”的？刷新时机在什么地方触发，是定时、接口返回码，还是页面可见性变化？
        
    - 追问2：无感刷新失败时，你们如何保证用户体验可控？是静默退登，还是给出提示？
        
- **如果回答泛**：
    
    - 追问3：在这些优化上线后，你们有没有对比过接口耗时、首屏时间或错误率？如果要现在补一套埋点，你会观测哪些指标？
        

---

### Q3：你提到「多个请求共同发送」「滚动一定距离后才触发请求」「搜索防抖」。这一整套请求策略是怎么设计出来的？

- **如果回答比较具体**（能说出并发控制、阈值、节流、防抖）：
    
    - 追问1：滚动触发加载时，你是怎么防止“快速上划导致多次请求叠加”的？有没有做到请求合并或取消？
        
    - 追问2：搜索防抖你一般会选多少 delay？在体验和服务器压力之间，你是如何权衡的？
        
- **如果回答泛**：
    
    - 追问3：如果现在发现列表在弱网环境下经常出现“重复请求同一页”的现象，你会从哪里开始排查？
        

---

## 三、智能化仓储系统（Uniapp + uView，小程序 / 移动端）

> 移动端管理订单入库/出库、流程审批、质量/库存管理；Uniapp + uView，负责核心需求分析和版本迭代、组件开发、动效实现、小程序主包/分包配置、从 0–1 框架搭建。

### Q1：在智能化仓储系统里，你用 Uniapp + uView 搭了移动端项目。你是怎样设计页面结构和状态管理，让它同时兼顾“业务复杂”和“多设备适配”的？

- **如果回答具体**（能讲到页面划分、全局状态、组件化、适配策略）：
    
    - 追问1：在小程序多端（不同机型、分辨率）上，你做过哪些适配处理？比如列表、表单、图表在小屏和大屏上的差异是什么？
        
    - 追问2：某个典型流程（如「入库申请 → 审批 → 入库完成」）在前端是如何组织的？你有做过流程可视化或引导吗？
        
- **如果回答泛**：
    
    - 追问3：你能挑一个你亲自写的“复杂页面”，从组件拆分、数据来源、状态管理三个角度讲一下你的设计吗？
        

---

### Q2：你提到负责从 0–1 的框架搭建和方案调研。Uniapp 项目从零开始，你当时在工程层面主要做了哪些约定和封装？

- **如果回答具体**（目录结构、路由封装、请求封装、主题/样式规范）：
    
    - 追问1：你们有没有封一层“接口服务层”？在 Uniapp 这种多端框架下，如何处理不同端 API 差异？
        
    - 追问2：主包 / 分包的拆分原则是怎样定的？在业务发展过程中有没有因为分包规划不合理而被迫重构？
        
- **如果回答泛**：
    
    - 追问3：如果再重来一次，你觉得在框架搭建阶段有什么是你会做得不一样的？
        

---

### Q3：你在这个项目中负责 UI 动效实现。移动端的动效你是如何在“流畅、低功耗”和“产品想要的炫酷效果”之间做取舍的？

- **如果回答具体**（transition/animation、骨架屏、列表过渡、loading 动画等）：
    
    - 追问1：有没有遇到过因为动效导致掉帧的情况？你是如何通过简化动画、减少重排/重绘来优化的？
        
    - 追问2：你会通过数据或用户反馈来判断某些动画是否是“有价值”的，而不是纯炫技吗？举个例子。
        
- **如果回答泛**：
    
    - 追问3：如果产品现在希望你给“审批列表 → 详情页”加一个“过场动效”，你会从哪几种方案里做选择？
        

---

## 四、多式联运数字驾驶舱（Vue3 大屏）

> Vue3 大屏展示；不同分辨率自适应布局；统一封装多种运输方式的下单维度；各维度展示总览、占比、货运量、趋势、列表；中间为中国地图；请求/路由/图表懒加载。

### Q1：多式联运驾驶舱是一个典型的大屏项目，你们是如何处理不同分辨率下的自适应布局的？具体方案是怎样的？

- **如果回答具体**（基准分辨率 + 等比缩放 / rem 布局 / 自适应栅格等）：
    
    - 追问1：在超宽屏或竖屏大屏上，等比缩放会让某些区域特别空或特别挤，你们有做过针对性优化吗？
        
    - 追问2：对 ECharts 或地图这种 Canvas 图层，在缩放后有没有遇到模糊或错位的问题？你们怎么处理的？
        
- **如果回答泛**：
    
    - 追问3：如果要在现有大屏的基础上拆成“左右两个物理屏联播”，你觉得自适应方案需要做哪些调整？
        

---

### Q2：这个项目对“多式联运、各种运输方式的下单维度”做了统一封装，并通过图表展示各种统计信息。你在“统一封装”这块具体做了什么？

- **如果回答具体**（统一数据模型、统一组件、统一配置）：
    
    - 追问1：各个运输方式的指标既有共性也有差异，你是如何在组件或配置层面抽象它们的？
        
    - 追问2：当新增一种运输方式（比如航空）时，前端一般只需改配置，还是需要写新的组件？
        
- **如果回答泛**：
    
    - 追问3：请你以「公路运输」为例，从接口数据 → 数据适配层 → 图表组件，完整讲一遍是如何走通的。
        

---

### Q3：你们对请求、路由、图表都做了懒加载。整体是按什么原则来划分和触发懒加载的？

- **如果回答具体**：
    
    - 追问1：图表懒加载是根据“页面可见性”还是“Tab 切换”来触发的？你们如何避免初次加载时所有图表同时加载导致的抖动？
        
    - 追问2：在请求懒加载时，有没有遇到“切换过快导致数据还没回来就离开页面”的问题？你是如何处理这类“浪费请求”的？
        
- **如果回答泛**：
    
    - 追问3：如果现在要在这个项目上加一个“实时推送告警”的模块，你会如何设计与懒加载策略的配合？
        

---

## 五、物流系统大屏（Vue2 + ECharts）

> Vue2 + vue-router + element-ui + ECharts；大屏展示世界地图/中国地图及各类图表，做适配；与后端商讨接口优化以改善断网、弱网情况下的体验。

### Q1：和前面的 Vue3 大屏相比，这个 Vue2 + ECharts 的物流大屏你主要负责了哪些部分？在适配和图表布局上你做过什么具体设计？

- **如果回答具体**：
    
    - 追问1：世界地图和中国地图在数据精度、投影、区域点击上有什么不同？你在使用 ECharts 时有做过什么特别处理？
        
    - 追问2：在大屏上做“响应式布局”，你对断点、比例、最小尺寸是如何设定和验证的？
        
- **如果回答泛**：
    
    - 追问3：假设现在要在这套 Vue2 大屏上加一个新的“物流路径回放动画”，你会从哪些技术选型开始考虑？
        

---

### Q2：你提到与后端开发对接，商讨接口优化，以提升断网、网速差情况下的体验。前端这边你具体做了哪些事情？

- **如果回答具体**（超时处理、降级方案、重试、缓存、本地兜底数据等）：
    
    - 追问1：你们有没有做过“本地缓存 + 接口返回后再增量更新”的策略？在大屏这种强调实时性的场景下，如何解释“数据可能略有延迟”？
        
    - 追问2：在弱网情况下，大屏自动轮播、图表自动刷新会不会带来额外压力？你们是如何调优刷新频率的？
        
- **如果回答泛**：
    
    - 追问3：如果监控上发现某些接口在高峰期响应特别慢，你会如何从前端视角给出优化建议？
- **Q9（TypeScript）：** 简历中提到“TS 定义公共字段”和“泛型”。请写一个简单的泛型工具类型（比如 Partial 或 Pick 的实现原理），或者解释一下 interface 和 type 在实际开发中选用的区别？
    
    - 考察点： 验证 TS 熟练度，防止仅用 any。
        
- **Q10（工程化）：** 你在简历中强调“熟练掌握 Webpack 构建工具”。在 Vue3 项目中，你是否对比过 Vite 和 Webpack 的区别？如果让你优化一个构建速度很慢的老项目，你会从哪几个维度入手（Loader/Plugin/Cache 等）？
    
    - 考察点： 构建工具理解深度。
# 3 符伟豪
## 一、智慧开班方案系统（Vue3 + TS + Pinia + AAD + ECharts）

> Vue3 + Vite + TS + Pinia + Element Plus + ECharts + Axios + AAD + Vue I18n，排班排线，可视化大屏、动态权限路由、Excel 上传下载、国际化等。

### Q1：你在智慧开班方案系统里是前端技术负责人，这个系统从 0 到 1 的整体前端架构大概是怎么设计的？

- **如果他回答得比较具体**（提到模块拆分、状态管理、路由结构、组件划分等）：
    
    - 追问1：你是如何划分「业务模块（排班配置、可视化大屏等）」和「通用基础模块（权限、状态、请求、组件库）」边界的？举一个你认为抽象得比较好的模块详细讲讲。
        
    - 追问2：随着迭代，业务复杂度上涨，你们有没有做过比较大的架构调整或重构？是什么问题促使你去改，最后怎么验证改动是值得的？
        
- **如果回答比较泛**（“就是 Vue3 + Pinia + 路由，按模块拆页面”）：
    
    - 追问3：那你挑一个你最熟的核心模块（比如排班排线配置页或总览大屏），从「路由进入 → 状态加载 → 组件渲染 → 接口交互」，顺着讲一遍整体调用链路。
        

---

### Q2：你们做了动态权限路由和多角色（管理员/用户）控制，还接了 AAD 登录。整套「认证 + 授权」在前端是怎么打通的？

- **如果回答具体**（能讲出：AAD 登录 → 获取 token / 用户信息 → 拉权限 → 生成路由/菜单）：
    
    - 追问1：权限控制你们做到什么粒度？只是菜单/路由级，还是有按钮级/字段级？在代码层面是怎么统一管理的？
        
    - 追问2：AAD token 过期、刷新、单点登录冲突这些边界问题你们是怎么处理的？有没有遇到过“用户被莫名其妙踢下线”的情况？
        
- **如果回答比较泛**（“用 AAD 登录，然后根据角色控制页面”）：
    
    - 追问3：假设现在新增一个角色“区域管理员”，他的权限介于管理员和普通用户之间，你会从哪里改起，怎么保证不影响已有角色？
        

---

### Q3：你提到封装了 Axios 拦截器统一处理 token 校验和错误提示，并用 Pinia + localStorage 做模块化状态管理，同时做了路由懒加载和多语言（中/繁/英）支持。你觉得这几个里面最有价值的一块是哪块？重点讲讲。

- **如果他选 Axios + 网络层封装**：
    
    - 追问1：拦截器里你们统一做了哪些逻辑？比如 token 注入、错误码映射、全局消息提示、重定向，这些是如何和业务代码解耦的？
        
    - 追问2：当某个接口在业务上需要“自定义错误处理”（例如静默失败、不弹出默认错误框）时，你们的封装是怎么支持局部覆盖的？
        
- **如果他选 Pinia + localStorage 状态管理**：
    
    - 追问1：哪些数据会进入 Pinia，哪些只放在组件或 URL 里？有没有踩过“状态滥用”的坑？
        
    - 追问2：使用 localStorage 做持久化时，你们如何处理版本升级、结构变更导致的反序列化问题？
        
- **如果他选 i18n / 路由懒加载**：
    
    - 追问1：多语言是「配置驱动」还是写在代码里的？如何保证 100% 页面都被覆盖而不会漏翻？
        
    - 追问2：路由懒加载前后，你们有实际对比过首屏时间、首包体积等指标吗？如果让你再往下挖性能，你会从哪里下手？
        

---

## 二、资产管理 App（WIPMM，Ionic + Angular + Capacitor）

> 使用 Ionic 重构原生 App，Angular Router + Capacitor 打包 iOS/Android，功能包括：资产详情、借还、移交、延期、领用/返库；Camera 拍照、条码扫码、WebSocket 通信、FCM 推送、多语言、AAD 登录等。

### Q1：你作为这个 App 的前端主要负责人，从环境搭建到最终打包都负责。能具体说说这个 Ionic + Angular 项目的工程结构和你做的关键封装吗？

- **如果回答具体**（目录结构、模块拆分、公共服务、组件化）：
    
    - 追问1：你们是如何抽象“资产相关流程”（借还、移交、延期、领用/返库）的？这些功能之间共用了哪些组件或 service？
        
    - 追问2：在多平台（iOS / Android）打包过程中，有没有遇到平台差异或插件兼容问题？当时是怎么排查和解决的？
        
- **如果回答泛**（“就按 Angular 标准方式搭项目，用 Ionic 组件搭页面”）：
    
    - 追问3：假设我要新增一个“资产报废申请”功能模块，你会如何在现有工程里落地，从路由、组件、service 到接口联调，大致流程讲一遍。
        

---

### Q2：你们用了 Camera 插件拍照、barcode-scanner 扫码做资产借还，并通过 WebSocket 实现用户间即时通信。整个借还流程在前端是怎样串起来的？

- **如果回答具体**（能讲出：扫码/拍照 → 调接口 → WebSocket 通知对方 → UI 更新）：
    
    - 追问1：扫码和拍照这类原生能力在失败、权限拒绝、摄像头不可用时，你们是怎么做好错误提示和兜底流程的？
        
    - 追问2：WebSocket 通信的消息格式、大致协议是谁定的？在断网、重连、消息丢失等场景下，你们是怎么确保“借还状态”不会错乱的？
        
- **如果回答泛**（“就扫个码发个请求，有消息就推过来”）：
    
    - 追问3：如果测试同事反馈“偶尔 A 端显示已归还但 B 端还是显示占用”，你会怎么排查问题是出在 WebSocket、后端逻辑，还是前端状态管理？
        

---

### Q3：这个 App 做了国际化（TranslateService）和 FCM 推送，还用 Infinite Scroll 优化列表。你自己觉得这里对体验提升最大的是哪一点？展开讲讲实现方式。

- **如果选 Infinite Scroll / 性能优化**：
    
    - 追问1：Infinite Scroll 的触发阈值、分页策略、加载状态（loading / no more data）你是怎么设计的？有没有处理“快速滚动导致多次触发”的问题？
        
    - 追问2：资产列表如果要支持搜索、筛选，你是怎么和无限滚动配合的？会不会造成逻辑复杂？
        
- **如果选 FCM 推送**：
    
    - 追问1：你们是怎么区分“高优先级通知（如审批通过）”和“一般提醒”的？在前端/客户端层面如何控制展示形式？
        
    - 追问2：用户是否可以在 App 内管理消息订阅（比如只要审批、不要系统公告）？如果要做，你会怎么设计？
        
- **如果选多语言**：
    
    - 追问1：你是怎么组织多国语言文案的？是按模块拆文件，还是统一一个大 JSON？
        
    - 追问2：在动态切换语言时，有没有遇到“部分组件文案不刷”或第三方组件不支持 i18n 的问题？怎么处理？
        

---

## 三、售后服务管理系统（ServiceMe，Vue + Quasar + Vuex）

> 将 .NET 老项目改造成前后端分离；Vue + VueRouter + Quasar + Vuex + axios + Sql Server；服务管理模块包含 Excel 上传/下载、数据查询操作等，并参与后端 .NET API 开发；公共组件、路由懒加载等。

### Q1：这个项目是把 .NET 老系统改成前后端分离。你参与的“服务管理模块”从原来的老页面迁移到 Vue + Quasar 的过程是怎样的？

- **如果回答具体**（能讲出：梳理老系统逻辑 → 拆接口 → 设计新页面 → 联调）：
    
    - 追问1：迁移过程中，如何保证“业务逻辑保持一致但前端体验升级”？有没有遇到过老系统逻辑很乱、文档缺失的情况，你是怎么补全和验证的？
        
    - 追问2：你参与 .NET API 开发，那你在前后端之间如何划分职责？比如某些复杂校验应该放前端还是放后端，你是怎么判断的？
        
- **如果回答泛**（“就是用 Vue 重写界面、调用新 API”）：
    
    - 追问3：举一个你印象最深的页面（比如服务查询及 Excel 导入导出），详细说说从老系统到新系统页面的变化，以及你具体负责了哪部分工作。
        

---

### Q2：服务管理模块涉及 Excel 上传/下载、数据查询和操作。前端这部分你是怎么设计上传、解析、错误提示和下载导出的体验的？

- **如果回答具体**（能讲出上传校验、后端解析、前端预览、错误行提示等）：
    
    - 追问1：针对大文件或大量数据行，你们是怎么控制上传速度、超时和用户等待体验的？有做进度条或分块上传吗？
        
    - 追问2：下载导出时，前端是直接接收文件流，还是跳转下载链接？在浏览器兼容性上有遇到过问题吗？
        
- **如果回答泛**（“就用 input 上传、接口返回结果”）：
    
    - 追问3：如果现在产品要求“Excel 导入失败时，用户能下载一份标出错误行的 Excel 回去修改”，你会从前端/后端两个角度分别怎么设计？
        

---

### Q3：你负责公共组件开发、路由管理、懒加载优化。你能举一个你写的公共组件，说明它是怎么设计来提高复用性的吗？

- **如果回答具体**（比如表格、搜索表单、弹窗等）：
    
    - 追问1：在这个公共组件里，你是如何用 props / emits / slot 控制“通用”和“定制”之间的平衡的？有遇到过后期需求越来越多导致组件变得臃肿的问题吗？
        
    - 追问2：这个组件后来有没有被其他项目或模块复用？在复用过程中，你觉得最初有哪些地方设计得不够好？
        
- **如果回答泛**（“就封装了一些按钮、弹窗之类的”）：
    
    - 追问3：假设让你现在设计一套“列表 + 搜索 + 分页”的通用模板组件，你会怎么拆分成几个组件、各自负责什么？
        

# 4 王国强
## 一、一搏设计移动端官网 H5（2025.08–2025.11）

> Vue3 + Vite + TS + Pinia + Vue-Router + Axios + Socket.io + Vant + SCSS；模块包括首页、设计师列表、预约、支付订单、聊天室等，使用 @vueuse/core 解决 postcss-px-to-viewport 行内样式适配问题，有支付引导、聊天室、评价卡片跨层级通信等。

### Q1：这个移动端官网涉及首页、设计师列表、预约、支付、聊天室等多个模块，你是怎么设计整体前端架构和状态管理的？

- **如果回答比较具体**（讲到 Pinia 模块划分、路由结构、全局/局部状态、组件抽象）：
    
    - 追问1：比如“预约 → 支付 → 评价”这一条业务链路里，你怎么避免在各个页面重复拉取/传递相同的数据？有没有做中间态缓存或全局 store？
        
    - 追问2：在 TS 下，你们对接口响应、组件 props、store state 有没有做比较严格的类型约束？有没有遇到类型和实际数据不匹配的坑？
        
- **如果回答比较泛**（“用 Pinia 管状态，按页面拆组件”）：
    
    - 追问3：那你就拿「预约设计师」这个流程详细说一下：从用户点击设计师，到提交预约、看到结果，中间前端的数据流和主要模块调用关系是怎样的？
        
### Q2：你实现了聊天室功能，用 Socket.io 做即时通信。能具体讲讲你在前端是怎么封装这部分的？

- **如果回答比较具体**（能说出 socket 实例封装、连接/重连、事件监听、消息列表管理等）：
    
    - 追问1：消息列表在本地是怎么管理的？比如未读计数、分页加载、历史记录，你有做过什么处理？
        
    - 追问2：在网络断开、重连、页面切换这些场景下，你如何保证不会出现“消息丢失或重复展示”？
        
- **如果回答比较泛**（“就 socket.emit/send 消息，socket.on 接收”）：
    
    - 追问3：如果现在线上出现一个 bug：用户 A 发送消息有时 B 收不到，但刷新页面后能看到完整历史，你会先从哪几个点开始排查？
        

---

### Q3：简历里提到你解决了 postcss-px-to-viewport 对行内样式不适配的问题，还实现了评价卡片跨层级通信。你觉得哪个问题最有代表性？展开讲讲你的解决过程。

- **如果选 px-to-viewport 行内样式适配**：
    
    - 追问1：你当时是如何定位到是“行内样式没有被插件处理”这个根因的？用到了哪些调试手段？
        
    - 追问2：使用 @vueuse/core / 其他方案之后，这个问题在后续迭代中还有没有引发新的坑，比如和第三方组件样式冲突？
        
- **如果选评价卡片跨层级通信**：
    
    - 追问1：你是如何在祖先组件和深层子组件之间实现“修改评价状态”的？用的是 props/emit、provide/inject，还是事件总线 / 全局 store？
        
    - 追问2：如果后面评价卡片模块要拆出去做成可复用组件库，这种跨层级通信你会怎么重构得更通用？
        
- **如果回答泛**：
    
    - 追问3：再假设一个类似问题：某个第三方组件内部用的是行内 style，px-to-viewport 也不生效，你会有哪几种备选思路？
        

---

## 二、堂胜 Prologis 智能园区（2024.12–2025.05）

> 数字化智能园区管理系统：后台管理 + 前台可视化大屏。后台：Vue2 + Vuex + Vue-Router + Element-UI + Axios；前台：Vue3 + ECharts + Spline + v-scale-screen + Qiankun，园区管理、车辆、物业、一体杆、员工、角色管理等，RBAC 权限、动态路由、el-permission 指令、树结构、懒加载等。

### Q1：这个项目同时有 Vue2 的后台和 Vue3 的可视化大屏，你具体负责的部分是哪些？这两块在工程上是怎么协同的？

- **如果回答比较具体**（能说出自己负责的模块、微前端/部署方式、两个项目的交互）：
    
    - 追问1：在权限和登录态上，后台和大屏之间是共享一套机制还是各自独立？如果用户在后台操作后切大屏，有没有单点登录或会话共享需求？
        
    - 追问2：你们用 Qiankun 接入大屏时，有没有遇到样式隔离、路由冲突或运行时依赖重复加载的问题？当时是怎么处理的？
        
- **如果回答比较泛**（“后台一些模块和大屏图表都有做”）：
    
    - 追问3：你挑一个你负责最深的模块（比如车辆管理/园区概况大屏），从接口、状态管理到 UI 渲染，详细讲一遍你的设计。
        

---

### Q2：你在后台做了 RBAC 权限管理，动态路由，el-permission 指令判断按钮权限。能具体说说这套权限体系在前端是怎么落地的吗？

- **如果回答具体**（角色→菜单→路由→按钮级指令，大概链路清楚）：
    
    - 追问1：动态路由和菜单渲染的流程是怎样的？用户登录后你们如何根据后端权限数据生成路由表？
        
    - 追问2：el-permission 指令的实现细节是怎样的？比如在 Vue2 响应式限制下，为什么需要用 `$set` 动态添加响应式数据？
        
- **如果回答泛**（“后端给权限，前端根据权限控制显示隐藏”）：
    
    - 追问3：假设现在新增一个“临时外包人员”角色，只能访问部分页面、部分按钮功能，你会从哪些代码和配置开始改，来支持这个新角色？
        

---

### Q3：大屏使用了 v-scale-screen 做适配，ECharts + Spline 做 2D/3D 展示。你在大屏的适配和可视化组件封装上做了什么工作？

- **如果回答具体**（基准分辨率 + scale、封装图表 hooks/组件、抽离业务逻辑）：
    
    - 追问1：在超宽/超高/竖屏这些“奇怪分辨率”上，v-scale-screen 的缩放会带来哪些 UI 问题？你做过哪些修正（字体最小值、布局微调等）？
        
    - 追问2：你们是如何把业务逻辑（如园区概况、收入分析）从图表组件里抽出来的？那段“把同一业务的 JS 放一个函数里 return 出来”的封装，具体长什么样？
        
- **如果回答泛**（“用 v-scale-screen 包一层就适配了，用 ECharts 写图表”）：
    
    - 追问3：如果现在要把这套大屏的“图表 + 适配方案”迁移到另一个新项目，你会抽成哪几层可复用模块？
        

---

## 三、科凡家具设计 小程序（2024.05–2024.07）

> uni-app + uni-ui + Sass + Vuex + fly-io；家具类小程序，首页、我的、分类、购物车、微信登录、订单、支付等；防抖搜索、上拉加载 / 下拉刷新、refreshToken 无感刷新、购物车角标更新、支付等。

### Q1：这个 uni-app 小程序是一整套电商流程。你在状态管理和网络请求封装上具体做了什么设计？

- **如果回答具体**（讲清 Vuex 模块划分、fly-io 封装、refreshToken、无感刷新）：
    
    - 追问1：哪些数据放在 Vuex，哪些只放在本地组件或本地存储？有没有踩过“把所有东西都丢 Vuex 导致难维护”的坑？
        
    - 追问2：refreshToken 的无感刷新你们是怎么触发的？在 token 刷新失败时你们怎么做用户体验处理？
        
- **如果回答泛**（“用 Vuex 管数据、fly-io 发请求”）：
    
    - 追问3：比如“购物车 + 订单列表”这一块，你是怎么确保页面切换时数据不会乱或者旧数据不会闪一下的？
        

---

### Q2：你提到分类页面支持上拉加载、下拉刷新，搜索框用了防抖。你是怎么平衡“响应速度”和“减少请求次数”的？

- **如果回答具体**（能说出具体节流/防抖时间、分页方案）：
    
    - 追问1：在上拉加载时，如何防止用户快速滑动导致多次请求叠加？是否有请求标记或取消机制？
        
    - 追问2：搜索防抖你一般选多长的 delay？在什么场景下你会调大或调小这个时间？
        
- **如果回答泛**：
    
    - 追问3：假设现在运营反馈“搜索结果有延迟感，用户觉得卡”，而后端压力又不能太大，你会提出什么折中的解决方案？
        

---

### Q3：你在项目中处理了购物车角标不更新、tabBar 页面跳转报错等问题。挑一个你觉得典型的 bug，讲讲你的排查思路。

- **如果提购物车角标不更新**：
    
    - 追问1：你最后发现核心原因是 watch 没有触发/生命周期问题吗？你是通过什么方式一步步缩小范围的？
        
    - 追问2：为防止类似问题重复出现，你后续有没有在架构或编码规范上做些预防措施，比如抽共用更新函数，统一放到 onLoad 中？
        
- **如果提 tabBar 页面 redirectTo 报错**：
    
    - 追问1：你是怎么想到通过判断当前页面是否 tabBar 来区分 redirectTo / switchTab 的？这个判断逻辑在哪里封装的？
        
    - 追问2：如果后面小程序又新增 tabBar 或改路由结构，这套判断是否还稳定？你会怎么设计得更不易出错？
        

---

## 四、杭门设计 小程序（2023.10–2024.02）

> 原生微信小程序 + Wechat-http + Dayjs + 腾讯位置服务 JS SDK；家居门类小程序，预约、店长推荐、附近门店、秒杀活动、售后等；封装 Wechat-http 请求库、登录检测组件、表单校验、refreshToken、分包、全局方法、腾讯位置服务、预约二维码等。

### Q1：你基于 Wechat-http 封装了可跨平台的请求工具，还做了拦截器统一处理参数和错误。这套封装的核心点是什么？

- **如果回答具体**（拦截器中处理 token、错误码、统一 header、自动带上某些参数）：
    
    - 追问1：在处理 token/refreshToken 时，你是如何避免在多请求并发时重复刷新或覆盖的问题？
        
    - 追问2：对于部分接口需要“完全自定义错误处理”的情况，你的封装是怎么兼容的？
        
- **如果回答泛**（“统一加参数、统一处理错误”）：
    
    - 追问3：如果现在要在这套请求工具上加“统一埋点上报接口耗时”，你会往哪一层加？怎么设计不会影响原有调用方式？
        

---

### Q2：这个项目里你做了登录状态检测组件、表单校验、分包加载、预约二维码等。你觉得哪一块最能体现你的设计能力？展开讲讲。

- **如果选登录检测组件**：
    
    - 追问1：这个组件是如何嵌入各个页面或流转节点的？是用自定义组件 + behavior，还是封在路由跳转前？
        
    - 追问2：登录态过期后的跳转和返回逻辑是怎么设计的？怎么避免“登录后回不去原页”的体验问题？
        
- **如果选预约二维码 / 分享**：
    
    - 追问1：二维码过期时你们是如何处理相关 UI（隐藏分享、按钮禁用等）的？这个过期逻辑是在前端判断还是依赖后端返回？
        
    - 追问2：你们有没有做过“二维码使用次数/有效期”的约束？在前端是如何展示给用户的？
        
- **如果回答泛**：
    
    - 追问3：如果现在给你一个新需求：“支持线下门店活动的一次性核销码”，你会参考哪个现有功能来设计？
        

---

## 五、一搏后台管理系统（2022.10–2023.05）

> 企业内部后台管理：员工管理、流程审批、任务、组织架构、考勤、会议、动态、权限管理等。Vue2 + Element-UI + Axios + Vuex + VueRouter + ECharts，RBAC 权限、动态路由、el-permission 指令、树结构、懒加载等。

### Q1：这是一个典型的企业后台系统，你在 RBAC 权限、动态路由和 el-permission 指令方面有不少经验。你能把这套权限体系从“登录”到“按钮显隐”完整讲一遍吗？

- **如果回答具体**：
    
    - 追问1：路由级权限和按钮级权限的数据结构是怎么设计的？前端如何从后端返回的数据中构建出路由和菜单树？
        
    - 追问2：el-permission 指令删除 DOM 节点的方式有没有带来什么副作用？比如 SSR、测试或后续接手同事理解成本？
        
- **如果回答泛**：
    
    - 追问3：如果我们现在要把这套权限方案替换成“基于资源的更细粒度权限（如字段级）”，你觉得现有体系哪些地方会先出问题？
        

---

### Q2：员工模块用了“左树右表”的布局，递归把列表数据转成树形结构。你在这块的实现细节是怎样的？为什么要用递归？

- **如果回答具体**（讲到根据 pid/id 构造树、递归出口、性能考虑）：
    
    - 追问1：当组织层级很深或节点很多时，这种递归构造有没有性能问题？你有做过尾递归优化或批量构造吗？
        
    - 追问2：在树节点选中联动右侧表格时，你是如何避免重复请求或重复渲染的？
        
- **如果回答泛**：
    
    - 追问3：如果后端能直接返回树结构，但部分节点需要前端再补充子节点（懒加载子树），你会怎么改造现有递归逻辑？
        

---

### Q3：这个后台也用了 ECharts 做数据可视化，并做了路由懒加载、图片懒加载、防抖节流等优化。你能挑一个你主导的性能优化点详细说说吗？

- **如果选路由懒加载 + 按需加载**：
    
    - 追问1：你们是如何拆分路由和 chunk 的？有没有用到 webpack 的分包策略来控制首屏体积？
        
    - 追问2：优化前后有做过 bundle 分析或首屏时间对比吗？结果怎样？
        
- **如果选防抖/节流或图片懒加载**：
    
    - 追问1：你在什么场景下使用了防抖/节流？是统一封装工具函数还是在组件内各自实现？
        
    - 追问2：图片懒加载在后台里通常不如 C 端明显，你们选择做这块优化的场景是什么？
        
- **如果回答泛**：
    
    - 追问3：假设现在监控发现“某些页面交互时长（TTI）偏高”，你会用哪些工具（如 Performance、Lighthouse）以及怎样的步骤来分析问题？
        

# 5 刘晴晴
## 一、UX 体验中心门户网站（2024.09–2025.10）

> Vue3 + TS + Vue Router + Element Plus + Vite + Node.js/Express + Nginx；车机交互视频查阅、成果展示、竞品分析上传、动态路由分享、视频播放器二次封装等。

### Q1：你从 0 到 1 主导了 UX 门户网站，从前端视角来看，你是怎么设计整体架构和模块划分的？

- **如果回答比较具体**（能说到模块边界、路由设计、状态管理/无状态、组件拆分）：
    
    - 追问1：车机交互视频查阅、成果展示、竞品分析这些模块之间，有哪些是复用的通用能力（比如上传、预览、权限）？你是怎么抽象和沉淀这些能力的？
        
    - 追问2：项目跑上线后，你们有做过比较大的架构调整吗？是出于性能、复杂度还是新需求？举一次你主导的调整说一下前后差异。
        
- **如果回答比较泛**（“用 Vue3+TS+Element 搭页面，按业务拆模块”）：
    
    - 追问3：那你挑一个你最熟的核心模块（比如“车机交互视频查阅”），从路由进入、数据获取、组件渲染、权限控制这条链路完整讲一遍。
        

---

### Q2：你提到二次封装了视频播放器组件，支持车机交互视频自动循环播放等。这个播放器封装的关键点有哪些？

- **如果回答比较具体**（讲到 props 设计、事件、全屏/进度控制、性能考量）：
    
    - 追问1：在车机场景的视频播放里，有没有遇到格式兼容、加载慢、首帧黑屏等问题？你是怎么处理或者规避的？
        
    - 追问2：播放器被不同页面复用时，业务会对控制条、自动播放、循环策略有不同需求，你是怎么通过配置/插槽满足这些差异的？
        
- **如果回答比较泛**（“封了个组件，外面直接用”）：
    
    - 追问3：如果现在产品希望在视频播放中间插入交互热点（比如点击跳到对应文档），你会如何在现有播放器封装基础上扩展？
        

---

### Q3：竞品分析报告上传和“动态路由分享”是这个门户的亮点之一。你是如何设计上传流程和分享链接的？尤其是动态路由这部分。

- **如果回答比较具体**（能说出路由参数编码、权限控制、分享入口）：
    
    - 追问1：分享链接给其他同事后，在权限控制上如何防止“越权访问”？前端有没有做额外校验或降级处理？
        
    - 追问2：竞品报告体积较大、资源多时，你们是否遇到过上传过程超时/失败的情况？做过哪些体验和稳健性优化？
        
- **如果回答比较泛**：
    
    - 追问3：假设现在要支持“外部合作方”也可以通过分享链接访问部分报告，你会从路由设计、鉴权和数据脱敏三个角度分别怎么设计？
        

---

## 二、设计规范网站搭建（1150 页面自动化获取，2024.08–2024.10）

> Node.js + Express + CORS + Nginx + Puppeteer + SingleFile；自动抓取 1150 个页面、下载视频资源、替换链接、本地化部署。

### Q1：你独立完成了 1150 个页面的自动化抓取和本地化，这个工具整体是怎么设计的？核心流程能梳理一下吗？

- **如果回答比较具体**（抓取队列、并发控制、错误重试、去重逻辑等）：
    
    - 追问1：1150 页如果不控制并发，容易压垮源站或本机，你们是如何控制并发、节奏和失败重试的？
        
    - 追问2：在抓取过程中，页面结构变更或异常页面（404、跳转）你是怎么检测和记录的？
        
- **如果回答比较泛**（“用 Puppeteer 循环打开页面，保存内容”）：
    
    - 追问3：如果后续页面新增到 2000+，你觉得现有抓取脚本在哪些地方会先扛不住？你会怎么优化？
        

---

### Q2：你写了脚本扫描页面下载视频，并进行 href 链接替换，使其本地化。前端/后端这块你是如何维护“原始链接”和“本地链接”之间的映射关系的？

- **如果回答比较具体**（提到映射表、路径规则、批处理等）：
    
    - 追问1：链接替换时，如何避免误伤，比如脚本或 JSON 里类似的字符串被错误替换？你有做过“只替换特定标签/属性”的限制吗？
        
    - 追问2：如果后续源站资源更新了，你们有没有机制做“增量同步”？还是一次性导入就冻结？
        
- **如果回答比较泛**：
    
    - 追问3：假如未来有第二套规范站点也要同样迁移，你会如何抽象这套工具，让它变成更通用的“站点资源本地化工具”？
        

---

## 三、Display & HUDDisplay Apk（2024.02–2024.07）

> Vue + JS + Capacitor，打包 Android 原生 App；多媒体资源分类展示、读取车机固定目录文件、多屏适配（中控屏 + HUD）。

### Q1：你用 Vue + Capacitor 做原生 Apk，用于车机中控屏和 AR-HUD 预览多媒体。前端工程和原生打包层面，你具体设计了哪些东西？

- **如果回答比较具体**（目录结构、路由、资源管理、Capacitor 插件）：
    
    - 追问1：读取车机固定目录文件这块，你们是如何封装原生能力的？前端调用时需要考虑权限、路径变更等什么问题？
        
    - 追问2：在调试阶段，你们是怎么统筹“浏览器开发调试”和“真机/车机调试”的？有没有搭配日志上报或远程调试方案？
        
- **如果回答比较泛**（“用 Vue 写界面，用 Capacitor 打包成 Apk”）：
    
    - 追问3：如果需要新增一种资源类型（比如 3D 模型文件）在车机上预览，你会如何在现有架构里接入，从前端展示到原生能力调用？
        

---

### Q2：多屏幕适配是这个项目的重点之一。你是如何保证同一套资源在中控屏和 HUD 上都能合理展示的？

- **如果回答比较具体**（说到不同分辨率/比例布局、字级/图标适配、布局策略）：
    
    - 追问1：HUD 的显示特点和中控屏很不一样，你们在 UI 布局、字体大小、信息层次上有哪些差异化处理？
        
    - 追问2：当后续新增新尺寸屏幕时，适配成本大不大？你会推荐用什么方式降低未来新增屏幕的成本（比如基于配置的布局系统等）？
        
- **如果回答比较泛**：
    
    - 追问3：如果设计师提出在 HUD 上增加更多实时信息展示，而空间有限，你从前端实现角度会怎么建议他们做取舍？
        

---

## 四、OCROneTestHarmony_Apk（2023.08–2024.01）

> HarmonyOS + ArkTS，Canvas 绘图，多卡证框选、时延与稳定性测试；封装 OCR/DocRefine 为独立 Har 模块，解决 SDK 升级兼容性问题。

### Q1：你独立完成了 OCR 测试 App 的 UI 架构和功能开发。ArkTS + HarmonyOS 的开发体验和传统 Vue/JS 相比，有哪些你觉得最不同的点？

- **如果回答比较具体**（组件模型、状态管理、声明式 UI、性能特点）：
    
    - 追问1：在多卡证框选、画布动态调整和离屏绘制上，你是如何利用 Canvas 和 ArkTS 特性的？有没有遇到过性能或卡顿问题？
        
    - 追问2：做时延与稳定性测试时，前端如何采集和展示这些指标？是完全依赖上游接口返回，还是前端也有时间打点逻辑？
        
- **如果回答比较泛**（“语法有点不一样，逻辑差不多”）：
    
    - 追问3：那你就挑“多卡证框选”这个功能，从用户操作 → Canvas 交互 → 数据提交这条链路，详细讲一遍实现。
        

---

### Q2：你把 OCR 和 DocRefine 封装成独立 Har 模块，来抵御 SDK 升级带来的影响。这个封装大概是怎么设计的？

- **如果回答比较具体**（模块边界、对外接口、适配层、版本兼容策略）：
    
    - 追问1：当上游 SDK 升级时，你们是如何验证 Har 模块的兼容性？有没有单测/集成测试？
        
    - 追问2：这个封装是否被其他项目复用？在复用过程中，有没有暴露出当初接口设计不合理的问题？
        
- **如果回答比较泛**：
    
    - 追问3：如果我们现在要把“图像预处理 + OCR + 结果可视化”打包成一个可复用组件，你会如何拆分模块与对外 API？
        

---

## 五、平安健康 CMS 移动端页面配置系统（2022.11–2023.07）

> Vue 全家桶 + Vue Router + Vuex + Element-UI；CMS-MANAGE 页面配置系统 + CRS H5 解析平台；拖拽组件、配置内容、跨源通信实现低代码可视化搭建；自研业务组件库（如 table-search）。

### Q1：这是一个低代码页面配置系统，cms-manage + crs 共同维护一套数据。你能详细讲一下“从拖拽组件配置到 CRS 渲染出页面”的完整链路吗？

- **如果回答比较具体**（组件 schema、配置 JSON、数据存储、跨源通信、解析渲染）：
    
    - 追问1：组件配置数据的结构是怎样的？如何兼容不同类型组件（广告位、表单、列表等）的差异？
        
    - 追问2：cms-manage 与 crs 之间的跨源通信具体用的什么方式？在安全性和性能上有做什么特别考虑？
        
- **如果回答比较泛**（“在 manage 里拖拽配置，在 crs 渲染页面”）：
    
    - 追问3：假设现在要新增一个“倒计时组件”，你会在 schema、配置 UI、CRS 解析渲染三个层面分别如何扩展？
        

---

### Q2：你参与了 table-search 组件库的开发，用于提升列表页开发效率和统一风格。这个组件大概是怎么设计的？

- **如果回答比较具体**（props/slots 设计、查询参数模型、表格与搜索联动）：
    
    - 追问1：组件如何处理不同列表的查询条件差异？是纯配置驱动，还是支持自定义 slot 嵌入复杂内容？
        
    - 追问2：在与 Vuex 或其他状态管理结合时，你们是否支持“记住上一次搜索条件”的功能？怎么实现？
        
- **如果回答比较泛**：
    
    - 追问3：如果我们要在项目里快速接一页“带搜索的活动列表”，你会如何利用 table-search，从 0 到上线走一遍？
        

---

## 六、平安健康内部员工管理 ERP 系统（2022.01–2022.10）

> Vue3 + Vue 全家桶 + Element Plus；组织架构基础数据（部门/用户/角色/权限），审批管理模块，Axios 二次封装，Vuex + localStorage 管审批流。

### Q1：在员工管理系统里，你负责组织架构模块（部门、用户、角色、权限）。前端是怎么建模这些实体以及它们之间关系的？

- **如果回答比较具体**（数据模型、树结构、列表视图、角色-权限映射）：
    
    - 追问1：角色和权限在前端的数据结构是什么样的？前端如何根据这些数据决定菜单/按钮的展示？
        
    - 追问2：当组织结构频繁变动时，你们如何避免前端缓存/状态里保留旧数据？
        
- **如果回答比较泛**：
    
    - 追问3：你能挑一个典型页面（比如角色管理），从接口返回的数据结构到页面展示，完整讲一下中间经过了哪些转换和封装吗？
        

---

### Q2：审批管理模块用 Vuex + localStorage 管理审批流。你是如何设计这套流转状态和本地持久化的？

- **如果回答比较具体**（状态机/审批节点、缓存策略、恢复流程）：
    
    - 追问1：审批流中如果用户中途关闭页面，下次打开如何恢复到合理的位置？使用 localStorage 时有没有版本控制或过期清理？
        
    - 追问2：在多端或多标签页同时打开时，审批状态如何避免冲突或“被覆盖”？
        
- **如果回答比较泛**：
    
    - 追问3：如果要支持“多级会签/或签”等更复杂的审批策略，你觉得在你当时的设计上，哪里会先扛不住？你会怎么改？
        

---

## 七、平安健康微信小程序（2021.04–2021.12）

> B2C 在线问诊平台小程序；vant-weapp + flyio + wxbarcode + mp-html；登录授权、用户信息收集、订单流程（分类、详情、SKU、购物车）、扫码下单、优惠券、分销推广海报。

### Q1：你负责小程序的登录授权和用户信息收集。你是如何设计整个“登录 + 授权 + 用户信息同步”的流程的？

- **如果回答比较具体**（调用微信登录、获取 code、后端换 token、用户信息/手机号授权、状态管理）：
    
    - 追问1：你们如何处理“拒绝授权”的场景？在产品和技术上是怎么平衡“信息获取”和“用户体验”的？
        
    - 追问2：登录态过期、token 刷新这些边界条件你们是怎么处理的？有没有做静默刷新或重新拉起登录？
        
- **如果回答比较泛**：
    
    - 追问3：如果现在要在这套登录流程里加上“风控校验”（比如设备指纹、多因子等），你觉得在前端大概要改哪些地方？
        

---

### Q2：你参与了订单流程（商品分类、详情、SKU 选择、购物车）和扫码下单。你觉得这里最复杂的一段业务逻辑在哪里？详细讲讲。

- **如果提 SKU 选择和优惠券/价格逻辑**：
    
    - 追问1：SKU 属性组合很多时，你们在前端如何处理“不可选/置灰”的逻辑？是前端计算还是依赖后端返回？
        
    - 追问2：优惠券对 VIP 和普通用户的支付逻辑不同，你是如何在代码里避免 if-else 地狱的？
        
- **如果提扫码下单**：
    
    - 追问1：扫码获取条码后，商品查找失败、库存不足、接口报错这些异常流程你们是怎么处理的？
        
    - 追问2：扫码频率高时，有没有做防抖或节流，避免同一商品被连续下单？
        
- **如果回答泛**：
    
    - 追问3：如果要给“下单-支付-发货”这一整条链路加上可观测埋点，你会选哪些关键节点来打点？
        
